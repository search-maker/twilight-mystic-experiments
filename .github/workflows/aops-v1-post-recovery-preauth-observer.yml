name: AOPS v1 post-recovery preauthorization observer
on:
  push:
    branches: [status/aops-v1-post-recovery-preauth-observer]
    paths: [.github/workflows/aops-v1-post-recovery-preauth-observer.yml]
permissions:
  actions: read
  contents: write
jobs:
  observe:
    if: github.run_attempt == 1
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Wait for exact-main preauthorization terminal state and publish compact checkpoint
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          target=452b386deafda29d404eb20c2573af15567dfc92
          found=''
          for i in $(seq 1 70); do
            gh api --paginate --slurp "repos/${GITHUB_REPOSITORY}/actions/workflows/aops-v1-preauthorization-audit.yml/runs?branch=main&event=push&per_page=100" > /tmp/pages.json
            python - <<'PY' > /tmp/match.json
          import json
          pages=json.load(open('/tmp/pages.json'))
          rows=[r for page in pages for r in page.get('workflow_runs',[]) if r.get('head_sha')=='452b386deafda29d404eb20c2573af15567dfc92']
          if len(rows)>1: raise SystemExit('multiple exact-head preauthorization runs')
          if rows:
              r=rows[0]
              print(json.dumps({k:r.get(k) for k in ('id','name','path','head_branch','head_sha','run_attempt','status','conclusion','created_at','updated_at')},sort_keys=True))
          PY
            if test -s /tmp/match.json; then
              status="$(python -c 'import json;print(json.load(open("/tmp/match.json"))["status"])')"
              if test "$status" = completed; then found=1; break; fi
            fi
            sleep 10
          done
          test -n "$found"
          python - <<'PY'
          import json
          r=json.load(open('/tmp/match.json'))
          assert r['path']=='.github/workflows/aops-v1-preauthorization-audit.yml'
          assert r['head_branch']=='main'
          assert r['head_sha']=='452b386deafda29d404eb20c2573af15567dfc92'
          assert r['run_attempt']==1
          open('/tmp/status.json','w').write(json.dumps(r,indent=2,sort_keys=True)+'\n')
          PY
          git checkout --orphan status/aops-v1-post-recovery-preauth-observer-result
          git rm -rf . >/dev/null 2>&1 || true
          mkdir -p observer-result
          cp /tmp/status.json observer-result/status.json
          git add observer-result/status.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m 'Record post-recovery AOPS preauthorization terminal status'
          git push -f origin HEAD:refs/heads/status/aops-v1-post-recovery-preauth-observer-result
