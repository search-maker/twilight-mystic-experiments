name: Review AVPS Recovery4 ordinal45 final dispatch choreography v22

on:
  pull_request:
    types: [opened]
    branches: [main]
    paths:
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22.yml
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml

permissions: {contents: read, actions: read, issues: read, pull-requests: read}
concurrency:
  group: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      INFRA_BASE: de3fafa052ff1d5b2f33ec0d4347c884d560d8b6
      INFRA_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v22
      INFRA_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22.yml
      PUBLISHER_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1
      PUBLISHER_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml
      V19_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v19.yml
      PR845_HEAD: e461c26ec78e9832ff43859bf63baf49464628b1
      V15_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v1.yml
      PR853_HEAD: 3a2ee4d92d634aec7575f813dea5adf38ba18400
      PR859_HEAD: 046c173babe19d5f57f1d96b73e7f27183853941
      PR859_TREE: f9ebcd12db6f9ffd45035db174702d8463444408
      PR859_BLOB: 47bc47b6cb319d703054507d2f2b90ad8b02ab35
      PR859_RUN: '33573792485'
      PR859_JOB: '100073226847'
      PR859_GENERIC: '33573792366'
      PR859_ARTIFACT: '9826088448'
      PR859_ARTIFACT_NAME: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v21
      PR859_ARTIFACT_DIGEST: sha256:9073044ca1e6edbc66a7eae93896cb815ca26c6858ac414b43d62d488623452e
      V21_BACKUP_DEFECT: '5502609968'
      V21_PRIMARY_DEFECT: '5502625148'
      ALLOCATION_MARKER: ORDINAL45_AVPS_V2_POSTCONSUMPTION_RECOVERY4_AUTHORIZATION_ALLOCATED_REVIEWED_NOT_DISPATCHED commit=6e095b4b1603c90dcee0943295909b30cd1b374d parent=5685d5c313071014a94c816ee26b129f1f17a7fc pr=774
      CONSUMED_MARKER: ORDINAL45_AVPS_V2_POSTCONSUMPTION_RECOVERY4_DISPATCH_CONSUMED
      DISPATCH_BRANCH: dispatch/aerosol-vertical-profile-sensitivity-v2-postconsumption-recovery4-ordinal-45
      SCIENCE_WORKFLOW: .github/workflows/avps-v2-postconsumption-recovery4-science.yml
      SCIENCE_WORKFLOW_BLOB: 2d210b3a161f6889352d600fb9222c974e728034
      RUNTIME_ADAPTER: runtime-avps-v2-recovery4-ordinal45-v1/runtime_adapter.py
      RUNTIME_ADAPTER_BLOB: e4b690c2969fd01ecde5b08f554e47ad50b40914
      EXECUTOR: runtime-avps-v2-recovery4-ordinal45-v1/executor.py
      EXECUTOR_BLOB: 7dc38e31c19701b1a1ceef300d75105cff179290
      AGGREGATOR: runtime-avps-v2-recovery4-ordinal45-v1/aggregator.py
      AGGREGATOR_BLOB: 6afc8b0d21c2b5553c251dc9ca5524c795ed2646
      RUNTIME_CONTRACT: runtime-avps-v2-recovery4-ordinal45-v1/runtime_contract.json
      RUNTIME_CONTRACT_BLOB: 2d5f71d80b5e573b86775c2806b80dcc1fbe02f4
      CANDIDATE_SEED_SHA256: ddded6b2d170ca2fac8d498bdba2887446c16995df0880d948fb2be00870b3de
      CANDIDATE_ROWS_SHA256: c439de417520b330c037e2628df02b6955f652563300aa5ef30477abf7661a98
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: '3.13'}

      - name: Bind V22 identity, rejected V21 evidence, and exact-only fence
        id: mode
        shell: bash
        env:
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = pull_request; test "${{ github.event.action }}" = opened; test "$GITHUB_RUN_ATTEMPT" = 1
          test "$(git rev-parse HEAD)" = "$EVENT_HEAD"; test "$(git rev-parse HEAD^)" = "$EVENT_BASE"; test "$(git rev-list --count "$EVENT_BASE"..HEAD)" = 1
          mapfile -t C < <(git diff --name-only "$EVENT_BASE"...HEAD); test "${#C[@]}" = 1
          if [ "${C[0]}" = "$INFRA_PATH" ]; then
            test "$EVENT_BASE" = "$INFRA_BASE"; test "$EVENT_BRANCH" = "$INFRA_BRANCH"; test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$INFRA_BASE"; echo mode=infra >> "$GITHUB_OUTPUT"
          elif [ "${C[0]}" = "$PUBLISHER_PATH" ]; then
            test "$EVENT_BRANCH" = "$PUBLISHER_BRANCH"; test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$EVENT_BASE"; test "$(git rev-parse "$EVENT_BASE:$INFRA_PATH")" = "$(git rev-parse "HEAD:$INFRA_PATH")"; echo mode=publisher >> "$GITHUB_OUTPUT"
          else exit 1; fi
          git fetch --no-tags origin "$PR845_HEAD" "$PR853_HEAD" "$PR859_HEAD"
          test "$(git rev-parse "$PR859_HEAD^{tree}")" = "$PR859_TREE"
          test "$(git rev-parse "$PR859_HEAD:.github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v21.yml")" = "$PR859_BLOB"
          if git ls-remote --exit-code --heads origin "$DISPATCH_BRANCH" >/dev/null 2>&1; then exit 1; fi
          gh api "repos/$GITHUB_REPOSITORY/pulls/859" > p.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR859_RUN" > r.json
          gh api "repos/$GITHUB_REPOSITORY/actions/jobs/$PR859_JOB" > j.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR859_GENERIC" > g.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR859_RUN/artifacts?per_page=100" > a.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > c.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/workflows/avps-v2-postconsumption-recovery4-science.yml/runs?event=workflow_dispatch&per_page=100" > s.json
          python - <<'PY'
          import json,os
          z=lambda p:json.load(open(p)); p,r,j,g=z('p.json'),z('r.json'),z('j.json'),z('g.json')
          if p.get('state')!='closed' or p.get('merged_at') is not None or p['head']['sha']!=os.environ['PR859_HEAD'] or p['base']['sha']!=os.environ['INFRA_BASE']:raise SystemExit('PR859 drift')
          if r['id']!=int(os.environ['PR859_RUN']) or r['head_sha']!=os.environ['PR859_HEAD'] or r['run_attempt']!=1 or r['conclusion']!='success':raise SystemExit('V21 dedicated drift')
          if j['id']!=int(os.environ['PR859_JOB']) or j['run_id']!=r['id'] or j['conclusion']!='success':raise SystemExit('V21 job drift')
          if g['id']!=int(os.environ['PR859_GENERIC']) or g['head_sha']!=os.environ['PR859_HEAD'] or g['run_attempt']!=1 or g['conclusion']!='success':raise SystemExit('V21 generic drift')
          A=[x for x in z('a.json').get('artifacts',[]) if int(x.get('id') or 0)==int(os.environ['PR859_ARTIFACT'])]
          if len(A)!=1 or A[0]['name']!=os.environ['PR859_ARTIFACT_NAME'] or A[0].get('digest')!=os.environ['PR859_ARTIFACT_DIGEST'] or A[0].get('expired'):raise SystemExit('V21 artifact drift')
          C=[x for q in z('c.json') for x in q]
          def exact(i,prefix,tokens):
              h=[x for x in C if int(x.get('id') or 0)==i]; b=str(h[0].get('body') or '') if len(h)==1 else ''
              if len(h)!=1 or not b.startswith(prefix) or (h[0].get('user') or {}).get('login')!='search-maker' or int((h[0].get('user') or {}).get('id') or 0)!=304153003 or h[0].get('author_association')!='OWNER':raise SystemExit('defect identity drift')
              for t in tokens:
                  if t not in b:raise SystemExit('defect token drift '+t)
          common=('pr=859',f'head={os.environ["PR859_HEAD"]}',f'base={os.environ["INFRA_BASE"]}',f'tree={os.environ["PR859_TREE"]}',f'reviewer_blob={os.environ["PR859_BLOB"]}',f'dedicated_run={os.environ["PR859_RUN"]}',f'artifact={os.environ["PR859_ARTIFACT"]}',f'artifact_digest={os.environ["PR859_ARTIFACT_DIGEST"]}',f'generic_run={os.environ["PR859_GENERIC"]}','QUARANTINE_DOWNSTREAM=false','NON_AUTHORIZING=true')
          exact(int(os.environ['V21_BACKUP_DEFECT']),'BACKUP_INDEPENDENT_AUDIT::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V21_PR859_FIXED_DEPTH_PRESENTATION_NORMALIZER_BYPASS',common+(f'job={os.environ["PR859_JOB"]}','conclusion=SUCCESS','generic_conclusion=SUCCESS'))
          exact(int(os.environ['V21_PRIMARY_DEFECT']),'INDEPENDENT_AUDIT::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V21_PR859_FIXED_DEPTH_PRESENTATION_NORMALIZER_BYPASS',common+(f'dedicated_job={os.environ["PR859_JOB"]}','dedicated_conclusion=SUCCESS','generic_conclusion=SUCCESS'))
          bodies=[str(x.get('body') or '').strip() for x in C]
          if sum(x==os.environ['ALLOCATION_MARKER'] for x in bodies)!=1 or any(x.startswith(os.environ['CONSUMED_MARKER']) for x in bodies):raise SystemExit('allocation/consumed drift')
          ends=set();beg=[]
          for x in C:
              first=str(x.get('body') or '').strip().splitlines(); first=first[0] if first else ''; fields={}
              for tok in first.replace('|',' ').split():
                  if '=' in tok:
                      k,v=tok.split('=',1); fields.setdefault(k,[]).append(v)
              if first.startswith('WRITE_QUIET_END'):
                  vals=[];raw=[]
                  for k in ('begin','beginComment','begin_comment'):
                      for v in fields.get(k,[]): raw.append(v); vals.append(int(v)) if v.isdigit() else None
                  if len(vals)!=len(raw) or len(set(vals))>1:raise SystemExit('invalid/conflicting END alias')
                  if vals:ends.add(vals[0])
              elif first.startswith('WRITE_QUIET_BEGIN'):beg.append(int(x['id']))
          if [x for x in beg if x not in ends]:raise SystemExit('unmatched fence')
          if [x for q in z('s.json') for x in q.get('workflow_runs',[])]:raise SystemExit('science exists')
          PY
          test "$(git rev-parse "HEAD:$SCIENCE_WORKFLOW")" = "$SCIENCE_WORKFLOW_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_ADAPTER")" = "$RUNTIME_ADAPTER_BLOB"
          test "$(git rev-parse "HEAD:$EXECUTOR")" = "$EXECUTOR_BLOB"
          test "$(git rev-parse "HEAD:$AGGREGATOR")" = "$AGGREGATOR_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_CONTRACT")" = "$RUNTIME_CONTRACT_BLOB"

      - uses: actions/download-artifact@v4
        with: {name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v21, path: v21, repository: '${{ github.repository }}', run-id: 33573792485, github-token: '${{ github.token }}'}

      - name: Build exact V22 publisher and closure-under-budget fixtures
        shell: bash
        env: {MODE: '${{ steps.mode.outputs.mode }}'}
        run: |
          set -euo pipefail
          cp v21/canonical-final-publisher.yml canonical-final-publisher.yml
          cp v21/publisher-closed-world-contract.json publisher-closed-world-contract.json
          cp v21/closed-world-hostile-fixtures.json closed-world-hostile-fixtures.json
          cp v21/v21-classifier-core.py old.py
          test "$(sha256sum canonical-final-publisher.yml|cut -d' ' -f1)" = "$(tr -d '\n' < v21/canonical-final-publisher.sha256)"
          python - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          R=json.load(open('v21/review-receipt.json'))
          if R.get('status')!='PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V21_ZERO_RUNTIME' or R.get('headSha')!=os.environ['PR859_HEAD'] or R.get('baseSha')!=os.environ['INFRA_BASE']:raise SystemExit('V21 receipt drift')
          for k in ('dispatchCreated','consumedMarkerCreated','workflowDispatchPerformed','solverExecutionPerformed','resultOpeningAuthorized','levelBAdmissionAuthorized','holdoutOpened','productionAuthorized','taylorOrJerusalemUsed','newMappingAuthorized'):
              if R.get(k) is not False:raise SystemExit('V21 authority drift '+k)
          got=hashlib.sha256(Path('old.py').read_text().strip('\n').encode()).hexdigest()
          if got!=Path('v21/v21-classifier.sha256').read_text().strip():raise SystemExit('V21 classifier digest drift')
          PY
          cat > v22-classifier-core.py <<'PY'
          import re
          B={5502130170,5502135669,5502242617,5502609968,5502625148}
          MAX_BODY_CHARS=131072
          MAX_LINE_CHARS=16384
          MAX_WORK=4096
          OPEN=('```','***','___','**','__','~~','`','*','_')
          def _prefix_once(s):
              old=s; s=re.sub(r'^\s*>\s?','',s,count=1)
              if s!=old:return s.strip(),True
              s=re.sub(r'^\s*(?:[-+*]|\d+[.)])\s+','',s,count=1)
              if s!=old:return s.strip(),True
              s=re.sub(r'^\s*\[[ xX]\]\s*','',s,count=1)
              if s!=old:return s.strip(),True
              s=re.sub(r'^\s{0,3}#{1,6}(?:\s+|$)','',s,count=1)
              if s!=old:return s.strip(),True
              s=s.strip()
              for m in OPEN:
                  if s.startswith(m):return s[len(m):].lstrip(),True
              return s,False
          def strip(line):
              s=str(line or '').strip()
              if len(s)>MAX_LINE_CHARS:return s,True
              work=0
              while work<MAX_WORK:
                  n,changed=_prefix_once(s)
                  if not changed:return s,False
                  s=n;work+=1
              _,residual=_prefix_once(s)
              return s,residual
          def _rest_once(r):
              s=r.lstrip(); changed=s!=r
              for m in OPEN:
                  if s.startswith(m):return s[len(m):].lstrip(),True
              n=re.sub(r'^[:=|\u2013\u2014-]+','',s,count=1)
              if n!=s:return n.lstrip(),True
              return s,changed
          def structured(line):
              s,exhausted=strip(line)
              if exhausted:return None,True
              m=re.match(r'^(DIRECTIVE|STATUS|STATE|DISPOSITION|ADMISSIBILITY|CLASSIFICATION)(?=$|[\s:=|\u2013\u2014*_~`-])',s,re.I)
              if not m:return None,False
              k=m.group(1).upper();r=s[m.end():];work=0
              while work<MAX_WORK:
                  n,changed=_rest_once(r)
                  if not changed:break
                  r=n;work+=1
              if work>=MAX_WORK:
                  _,residual=_rest_once(r)
                  if residual:return None,True
              return k+((' '+r.strip()) if r.strip() else ''),False
          def avps_governance_blocker(body,cid=None):
              if cid is not None and int(cid) in B:return False
              t=str(body or '');u=t.upper()
              if 'AVPS' not in u:return False
              if len(t)>MAX_BODY_CHARS:return True
              if any(len(x)>MAX_LINE_CHARS for x in t.splitlines()):return True
              if any(x in u for x in ('CONFIRMED_DEFECT','MATERIAL_RISK','QUARANTINE_DOWNSTREAM=TRUE','FAIL_CLOSED','FAIL-CLOSED','BLOCKER')):return True
              raw=[x for x in t.splitlines() if x.strip()];surfaces=[]
              if raw:
                  first,exhausted=strip(raw[0])
                  if exhausted:return True
                  surfaces.append(first)
              for y in raw[1:]:
                  x,exhausted=structured(y)
                  if exhausted:return True
                  if x:surfaces.append(x)
              for x in map(str.upper,surfaces):
                  if re.search(r'NOT[\s_-]+ADMISSIBLE',x) or re.search(r'DO[\s_-]+NOT[\s_-]+USE',x) or re.search(r'COORDINATOR[\s_-]+RECOVERY',x) or re.search(r'SUPERSEDING[\s_-]+DIRECTIVE',x) or re.search(r'(?<![A-Z0-9_])CLOSED(?![A-Z0-9_])',x):return True
              return False
          PY
          python - <<'PY'
          import hashlib,importlib.util,json,os
          from pathlib import Path
          sp=importlib.util.spec_from_file_location('c','v22-classifier-core.py');m=importlib.util.module_from_spec(sp);sp.loader.exec_module(m)
          p=Path('canonical-final-publisher.yml');raw=p.read_text();old=Path('old.py').read_text().strip('\n');new=Path('v22-classifier-core.py').read_text().strip('\n')
          oh='\n'.join(('          '+x if x else '') for x in old.splitlines())+'\n';nh='\n'.join(('          '+x if x else '') for x in new.splitlines())+'\n'
          if raw.count(oh)!=1:raise SystemExit('classifier patch cardinality')
          if raw.count('KNOWN_DEFECT=5501505154')!=1:raise SystemExit('cutoff patch cardinality')
          out=raw.replace(oh,nh,1).replace('KNOWN_DEFECT=5501505154','KNOWN_DEFECT=5502625148',1)
          F=[
            'AVPS_X\n'+'> '*16+'STATUS: NOT_ADMISSIBLE',
            'AVPS_X\n'+'> '*17+'STATUS: NOT_ADMISSIBLE',
            'AVPS_X\n'+'> '*128+'STATUS: NOT_ADMISSIBLE',
            'AVPS_X\n> - ### **`_STATUS_`**: NOT_ADMISSIBLE',
            'AVPS_X\n- [ ] ***STATUS***: NOT_ADMISSIBLE',
            'AVPS_X\n1. > __DISPOSITION__: DO NOT USE',
            'AVPS_X\n> ### ~~STATE~~: CLOSED',
            'AVPS_X\n**CLASSIFICATION:** SUPERSEDING DIRECTIVE',
            'AVPS_X\n**STATUS: NOT_ADMISSIBLE',
            'AVPS_X\n'+'> '*(m.MAX_WORK+1)+'STATUS: NOT_ADMISSIBLE',
            'AVPS_X\n'+'x'*(m.MAX_LINE_CHARS+1)]
          G=[
            'AVPS_OK\nHistorical `STATUS`: NOT_ADMISSIBLE example.',
            'AVPS_OK\n- [ ] Historical **STATUS:** NOT_ADMISSIBLE example.',
            'AVPS_OK\nSTATUSISH: NOT_ADMISSIBLE',
            'ACCEPT_AVPS_CLOSED_WORLD_PROOF',
            'ARM DO NOT USE',
            'ARM\n'+'x'*(m.MAX_LINE_CHARS+1)]
          for x in F:
              if not m.avps_governance_blocker(x):raise SystemExit('fatal miss '+x[:120])
          for x in G:
              if m.avps_governance_blocker(x):raise SystemExit('benign overblock '+x[:120])
          for i in sorted(m.B):
              if m.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',i):raise SystemExit('bound predecessor not suppressed')
          if not m.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',5502625149):raise SystemExit('unbound fail-open')
          cd=hashlib.sha256(new.encode()).hexdigest();d=hashlib.sha256(out.encode()).hexdigest();p.write_text(out);Path('canonical-final-publisher.sha256').write_text(d+'\n');Path('v22-classifier.sha256').write_text(cd+'\n')
          q=json.load(open('publisher-closed-world-contract.json'));q.update({'liveGovernanceClassifier':'closure-under-budget-v22','classifierSourceSha256':cd,'exactSuppressedPredecessorIds':sorted(m.B),'newerBlockerBaseline':5502625148,'canonicalPublisherSha256':d,'presentationMaxBodyChars':m.MAX_BODY_CHARS,'presentationMaxLineChars':m.MAX_LINE_CHARS,'presentationMaxWork':m.MAX_WORK,'residualPresentationFailClosed':True});Path('publisher-closed-world-contract.json').write_text(json.dumps(q,sort_keys=True,indent=2)+'\n')
          Path('v22-fixtures.json').write_text(json.dumps({'fatalCount':len(F),'benignCount':len(G),'fatal':F[:9]+['<budget-exhaustion>','<oversized-avps>'],'benign':G[:5]+['<oversized-non-avps>'],'exactSuppressedPredecessorIds':sorted(m.B)},sort_keys=True,indent=2)+'\n')
          if os.environ['MODE']=='publisher' and Path(os.environ['PUBLISHER_PATH']).read_text()!=out:raise SystemExit('publisher byte mismatch')
          PY

      - name: Replay V19 global scan and fresh V22 closure
        shell: bash
        env:
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ github.token }}
          MODE: ${{ steps.mode.outputs.mode }}
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          git show "$PR853_HEAD:$V19_PATH" > q.yml
          python - <<'PY'
          from pathlib import Path
          r=Path('q.yml').read_text();a=r.index('      - name: Replay exact V15 repository-global scan with bound V19 cutoff\n');s=r.index('        run: |\n',a)+len('        run: |\n');e=min(x for x in (r.find('\n      - name:',s),r.find('\n      - uses:',s)) if x!=-1);L=r[s:e].splitlines();x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n';o="if k=='PR840_DEFECT_COMMENT': v='5501505154'";n="if k=='PR840_DEFECT_COMMENT': v='5502625148'";assert x.count(o)==1;Path('scan.sh').write_text(x.replace(o,n,1))
          PY
          bash scan.sh
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > fc.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/workflows/avps-v2-postconsumption-recovery4-science.yml/runs?event=workflow_dispatch&per_page=100" > fs.json
          if git ls-remote --exit-code --heads origin "$DISPATCH_BRANCH" >/dev/null 2>&1; then exit 1; fi
          python - <<'PY'
          import importlib.util,json,os,subprocess
          from pathlib import Path
          sp=importlib.util.spec_from_file_location('c','v22-classifier-core.py');m=importlib.util.module_from_spec(sp);sp.loader.exec_module(m)
          C=[x for q in json.load(open('fc.json')) for x in q];base=5502625148
          for x in C:
              i=int(x.get('id') or 0)
              if i>base and m.avps_governance_blocker(x.get('body'),i):raise SystemExit(f'new blocker {i}')
          B=[str(x.get('body') or '').strip() for x in C]
          if sum(x==os.environ['ALLOCATION_MARKER'] for x in B)!=1 or any(x.startswith(os.environ['CONSUMED_MARKER']) for x in B):raise SystemExit('allocation/consumed drift')
          ends=set();beg=[]
          for x in C:
              first=str(x.get('body') or '').strip().splitlines();first=first[0] if first else '';fields={}
              for tok in first.replace('|',' ').split():
                  if '=' in tok:
                      k,v=tok.split('=',1);fields.setdefault(k,[]).append(v)
              if first.startswith('WRITE_QUIET_END'):
                  vals=[];raw=[]
                  for k in ('begin','beginComment','begin_comment'):
                      for v in fields.get(k,[]):raw.append(v);vals.append(int(v)) if v.isdigit() else None
                  if len(vals)!=len(raw) or len(set(vals))>1:raise SystemExit('invalid/conflicting END alias after scan')
                  if vals:ends.add(vals[0])
              elif first.startswith('WRITE_QUIET_BEGIN'):beg.append(int(x['id']))
          if [x for x in beg if x not in ends]:raise SystemExit('unmatched WRITE_QUIET_BEGIN after scan')
          if [x for q in json.load(open('fs.json')) for x in q.get('workflow_runs',[])]:raise SystemExit('science exists')
          S=json.load(open('repository-global-seed-scan.json'))
          if S.get('repositoryGlobalCollisionCount',S.get('collisionCount'))!=0 or S.get('repositoryGlobalDoubleEnumerationStable') is not True or S.get('auditedBranchHeadMatchesRepositoryHead') is not True:raise SystemExit('global scan drift')
          R={'schemaVersion':18,'status':'PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V22_ZERO_RUNTIME','mode':os.environ['MODE'],'headSha':os.environ['EVENT_HEAD'],'baseSha':os.environ['EVENT_BASE'],'reviewerBlob':subprocess.check_output(['git','hash-object',os.environ['INFRA_PATH']],text=True).strip(),'predecessorDefects':[5502609968,5502625148],'newerBlockerBaseline':base,'exactSuppressedPredecessorIds':sorted(m.B),'classifierSourceSha256':Path('v22-classifier.sha256').read_text().strip(),'canonicalPublisherSha256':Path('canonical-final-publisher.sha256').read_text().strip(),'presentationMaxBodyChars':m.MAX_BODY_CHARS,'presentationMaxLineChars':m.MAX_LINE_CHARS,'presentationMaxWork':m.MAX_WORK,'residualPresentationFailClosed':True,'scientificOrdinal':45,'candidateSeedCanonicalSha256':os.environ['CANDIDATE_SEED_SHA256'],'candidateRowsCanonicalSha256':os.environ['CANDIDATE_ROWS_SHA256'],'caseCount':360,'commonRandomNumberGroupCount':72,'statesPerGroup':5,'photonHistoriesPerCase':20000000,'dispatchCreated':False,'consumedMarkerCreated':False,'workflowDispatchPerformed':False,'solverExecutionPerformed':False,'resultOpeningAuthorized':False,'levelBAdmissionAuthorized':False,'holdoutOpened':False,'productionAuthorized':False,'taylorOrJerusalemUsed':False,'newMappingAuthorized':False};Path('review-receipt.json').write_text(json.dumps(R,sort_keys=True,indent=2)+'\n')
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22
          path: |
            review-receipt.json
            canonical-final-publisher.yml
            canonical-final-publisher.sha256
            publisher-closed-world-contract.json
            closed-world-hostile-fixtures.json
            v22-classifier-core.py
            v22-classifier.sha256
            v22-fixtures.json
            tracked-seed-scan.json
            repository-global-seed-scan.json
            repository-global-scan-success-attempt.txt
            final-global-scan-attempts/
          if-no-files-found: error
          compression-level: 0
