name: Review AVPS Recovery4 ordinal45 final dispatch choreography v24

on:
  pull_request:
    types: [opened]
    branches: [main]
    paths:
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v24.yml
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml

permissions: {contents: read, actions: read, issues: read, pull-requests: read}
concurrency:
  group: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v24-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      INFRA_BASE: de3fafa052ff1d5b2f33ec0d4347c884d560d8b6
      INFRA_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v24
      INFRA_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v24.yml
      PUBLISHER_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1
      PUBLISHER_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml
      PR845_HEAD: e461c26ec78e9832ff43859bf63baf49464628b1
      V15_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v1.yml
      PR853_HEAD: 3a2ee4d92d634aec7575f813dea5adf38ba18400
      V19_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v19.yml
      PR863_HEAD: d5cc6a549403b2bdfe42512fc9c37e4de1fa3f25
      PR863_TREE: 9caab7645caeb30e13e625a0b45cd71750a70206
      PR863_BLOB: 48eb8bd0ea7ac2082beaceb3831b2785d601bc8c
      PR863_ARTIFACT: '9827324743'
      PR863_ARTIFACT_NAME: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22
      PR863_ARTIFACT_DIGEST: sha256:9f31e55472fec84f4293e71158eb0a6db076e453567786a41483c27825045e4a
      PR864_HEAD: 1d4bf9dae0edb9969ef05ffa1584cf6cb3eba3eb
      PR864_TREE: 0247d8296ae75381f253f414e64c5081cc59b56c
      PR864_BLOB: 82797fe87c09c918fb7e62630c45a442a9d77a02
      PR864_RUN: '33578672969'
      PR864_JOB: '100088097811'
      PR864_GENERIC: '33578672913'
      PR864_DEFECT: '5502874645'
      V23_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v23.yml
      ALLOCATION_MARKER: ORDINAL45_AVPS_V2_POSTCONSUMPTION_RECOVERY4_AUTHORIZATION_ALLOCATED_REVIEWED_NOT_DISPATCHED commit=6e095b4b1603c90dcee0943295909b30cd1b374d parent=5685d5c313071014a94c816ee26b129f1f17a7fc pr=774
      CONSUMED_MARKER: ORDINAL45_AVPS_V2_POSTCONSUMPTION_RECOVERY4_DISPATCH_CONSUMED
      DISPATCH_BRANCH: dispatch/aerosol-vertical-profile-sensitivity-v2-postconsumption-recovery4-ordinal-45
      SCIENCE_WORKFLOW: .github/workflows/avps-v2-postconsumption-recovery4-science.yml
      SCIENCE_WORKFLOW_BLOB: 2d210b3a161f6889352d600fb9222c974e728034
      RUNTIME_ADAPTER: runtime-avps-v2-recovery4-ordinal45-v1/runtime_adapter.py
      RUNTIME_ADAPTER_BLOB: e4b690c2969fd01ecde5b08f554e47ad50b40914
      EXECUTOR: runtime-avps-v2-recovery4-ordinal45-v1/executor.py
      EXECUTOR_BLOB: 7dc38e31c19701b1a1ceef300d75105cff179290
      AGGREGATOR: runtime-avps-v2-recovery4-ordinal45-v1/aggregator.py
      AGGREGATOR_BLOB: 6afc8b0d21c2b5553c251dc9ca5524c795ed2646
      RUNTIME_CONTRACT: runtime-avps-v2-recovery4-ordinal45-v1/runtime_contract.json
      RUNTIME_CONTRACT_BLOB: 2d5f71d80b5e573b86775c2806b80dcc1fbe02f4
      CANDIDATE_SEED_SHA256: ddded6b2d170ca2fac8d498bdba2887446c16995df0880d948fb2be00870b3de
      CANDIDATE_ROWS_SHA256: c439de417520b330c037e2628df02b6955f652563300aa5ef30477abf7661a98
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: '3.13'}

      - name: Bind V24 identity, rejected V23 evidence, and exact-only fence
        id: mode
        shell: bash
        env:
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = pull_request
          test "${{ github.event.action }}" = opened
          test "$GITHUB_RUN_ATTEMPT" = 1
          test "$(git rev-parse HEAD)" = "$EVENT_HEAD"
          test "$(git rev-parse HEAD^)" = "$EVENT_BASE"
          test "$(git rev-list --count "$EVENT_BASE"..HEAD)" = 1
          mapfile -t C < <(git diff --name-only "$EVENT_BASE"...HEAD); test "${#C[@]}" = 1
          if [ "${C[0]}" = "$INFRA_PATH" ]; then
            test "$EVENT_BASE" = "$INFRA_BASE"; test "$EVENT_BRANCH" = "$INFRA_BRANCH"; test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$INFRA_BASE"; echo mode=infra >> "$GITHUB_OUTPUT"
          elif [ "${C[0]}" = "$PUBLISHER_PATH" ]; then
            test "$EVENT_BRANCH" = "$PUBLISHER_BRANCH"; test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$EVENT_BASE"; test "$(git rev-parse "$EVENT_BASE:$INFRA_PATH")" = "$(git rev-parse "HEAD:$INFRA_PATH")"; echo mode=publisher >> "$GITHUB_OUTPUT"
          else exit 1; fi
          git fetch --no-tags origin "$PR863_HEAD" "$PR864_HEAD" "$PR853_HEAD" "$PR845_HEAD"
          test "$(git rev-parse "$PR863_HEAD^{tree}")" = "$PR863_TREE"
          test "$(git rev-parse "$PR863_HEAD:.github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22.yml")" = "$PR863_BLOB"
          test "$(git rev-parse "$PR864_HEAD^{tree}")" = "$PR864_TREE"
          test "$(git rev-parse "$PR864_HEAD:$V23_PATH")" = "$PR864_BLOB"
          if git ls-remote --exit-code --heads origin "$DISPATCH_BRANCH" >/dev/null 2>&1; then exit 1; fi
          gh api "repos/$GITHUB_REPOSITORY/pulls/864" > p.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR864_RUN" > r.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/runs/$PR864_RUN/jobs?per_page=100" > jobs.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR864_GENERIC" > g.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/33577350181/artifacts?per_page=100" > a.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > c.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/workflows/avps-v2-postconsumption-recovery4-science.yml/runs?event=workflow_dispatch&per_page=100" > s.json
          python - <<'PY'
          import json,os
          z=lambda p:json.load(open(p));p,r,g=z('p.json'),z('r.json'),z('g.json')
          if p.get('state')!='closed' or p.get('merged_at') is not None or p['head']['sha']!=os.environ['PR864_HEAD'] or p['base']['sha']!=os.environ['INFRA_BASE']:raise SystemExit('PR864 drift')
          if r['id']!=int(os.environ['PR864_RUN']) or r['head_sha']!=os.environ['PR864_HEAD'] or r['run_attempt']!=1 or r['conclusion']!='failure':raise SystemExit('V23 dedicated drift')
          jobs=[j for page in z('jobs.json') for j in page.get('jobs',[])]; h=[j for j in jobs if int(j.get('id') or 0)==int(os.environ['PR864_JOB'])]
          if len(h)!=1 or h[0].get('run_id')!=r['id'] or h[0].get('conclusion')!='failure':raise SystemExit('V23 job drift')
          st={x.get('name'):x.get('conclusion') for x in h[0].get('steps',[])}
          expected={'Bind V23 identity, rejected V22 evidence, and exact-only fence':'success','Run actions/download-artifact@v4':'success','Build exact V23 publisher and structured-firstline fixtures':'success','Replay V19 global scan and fresh V23 closure':'failure','Run actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a':'skipped'}
          for k,v in expected.items():
            if st.get(k)!=v:raise SystemExit('V23 step drift '+k)
          if g['id']!=int(os.environ['PR864_GENERIC']) or g['head_sha']!=os.environ['PR864_HEAD'] or g['run_attempt']!=1:raise SystemExit('V23 generic identity drift')
          A=[x for x in z('a.json').get('artifacts',[]) if int(x.get('id') or 0)==int(os.environ['PR863_ARTIFACT'])]
          if len(A)!=1 or A[0]['name']!=os.environ['PR863_ARTIFACT_NAME'] or A[0].get('digest')!=os.environ['PR863_ARTIFACT_DIGEST'] or A[0].get('expired'):raise SystemExit('V22 artifact drift')
          C=[x for q in z('c.json') for x in q]; hits=[x for x in C if int(x.get('id') or 0)==int(os.environ['PR864_DEFECT'])]
          if len(hits)!=1:raise SystemExit('V23 defect cardinality')
          c=hits[0];b=str(c.get('body') or '');u=c.get('user') or {}
          pref='COORDINATOR::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V23_PR864_REPLAY_ENVIRONMENT_DEPENDENCY_UNBOUND'
          toks=('pr=864',f'head={os.environ["PR864_HEAD"]}',f'base={os.environ["INFRA_BASE"]}',f'tree={os.environ["PR864_TREE"]}',f'reviewer_blob={os.environ["PR864_BLOB"]}',f'dedicated_run={os.environ["PR864_RUN"]}',f'dedicated_job={os.environ["PR864_JOB"]}','dedicated_attempt=1','dedicated_conclusion=FAILURE','failing_step=Replay V19 global scan and fresh V23 closure',f'generic_run={os.environ["PR864_GENERIC"]}','QUARANTINE_DOWNSTREAM=false','NON_AUTHORIZING=true','NO_DISPATCH=true','NO_CONSUMED=true','NO_SCIENCE=true')
          if not b.startswith(pref) or u.get('login')!='search-maker' or int(u.get('id') or 0)!=304153003 or c.get('author_association')!='OWNER':raise SystemExit('V23 defect identity')
          for t in toks:
            if t not in b:raise SystemExit('V23 defect token '+t)
          bodies=[str(x.get('body') or '').strip() for x in C]
          if sum(x==os.environ['ALLOCATION_MARKER'] for x in bodies)!=1 or any(x.startswith(os.environ['CONSUMED_MARKER']) for x in bodies):raise SystemExit('allocation/consumed drift')
          ends=set();beg=[]
          for x in C:
            first=str(x.get('body') or '').strip().splitlines();first=first[0] if first else '';fields={}
            for tok in first.replace('|',' ').split():
              if '=' in tok:
                k,v=tok.split('=',1);fields.setdefault(k,[]).append(v)
            if first.startswith('WRITE_QUIET_END'):
              vals=[];raw=[]
              for k in ('begin','beginComment','begin_comment'):
                for v in fields.get(k,[]):raw.append(v);vals.append(int(v)) if v.isdigit() else None
              if len(vals)!=len(raw) or len(set(vals))>1:raise SystemExit('invalid END')
              if vals:ends.add(vals[0])
            elif first.startswith('WRITE_QUIET_BEGIN'):beg.append(int(x['id']))
          if [x for x in beg if x not in ends]:raise SystemExit('unmatched fence')
          if [x for q in z('s.json') for x in q.get('workflow_runs',[])]:raise SystemExit('science exists')
          PY
          test "$(git rev-parse "HEAD:$SCIENCE_WORKFLOW")" = "$SCIENCE_WORKFLOW_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_ADAPTER")" = "$RUNTIME_ADAPTER_BLOB"
          test "$(git rev-parse "HEAD:$EXECUTOR")" = "$EXECUTOR_BLOB"
          test "$(git rev-parse "HEAD:$AGGREGATOR")" = "$AGGREGATOR_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_CONTRACT")" = "$RUNTIME_CONTRACT_BLOB"

      - uses: actions/download-artifact@v4
        with: {name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22, path: v22, repository: '${{ github.repository }}', run-id: 33577350181, github-token: '${{ github.token }}'}

      - name: Build exact V24 publisher from exact V23 classifier semantics
        shell: bash
        env: {MODE: '${{ steps.mode.outputs.mode }}'}
        run: |
          set -euo pipefail
          cp v22/canonical-final-publisher.yml canonical-final-publisher.yml
          cp v22/publisher-closed-world-contract.json publisher-closed-world-contract.json
          cp v22/closed-world-hostile-fixtures.json closed-world-hostile-fixtures.json
          cp v22/v22-classifier-core.py old.py
          test "$(sha256sum canonical-final-publisher.yml|cut -d' ' -f1)" = "$(tr -d '\n' < v22/canonical-final-publisher.sha256)"
          git show "$PR864_HEAD:$V23_PATH" > v23.yml
          python - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          R=json.load(open('v22/review-receipt.json'))
          if R.get('status')!='PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V22_ZERO_RUNTIME' or R.get('headSha')!=os.environ['PR863_HEAD'] or R.get('baseSha')!=os.environ['INFRA_BASE'] or R.get('reviewerBlob')!=os.environ['PR863_BLOB']:raise SystemExit('V22 receipt drift')
          for k in ('dispatchCreated','consumedMarkerCreated','workflowDispatchPerformed','solverExecutionPerformed','resultOpeningAuthorized','levelBAdmissionAuthorized','holdoutOpened','productionAuthorized','taylorOrJerusalemUsed','newMappingAuthorized'):
            if R.get(k) is not False:raise SystemExit('V22 authority drift '+k)
          raw=Path('v23.yml').read_text();a="          cat > v23-classifier-core.py <<'PY'\n";b="\n          PY\n          python - <<'PY'\n"
          if raw.count(a)!=1:raise SystemExit('V23 helper start drift')
          s=raw.index(a)+len(a);e=raw.index(b,s);lines=raw[s:e].splitlines()
          if any(x and not x.startswith('          ') for x in lines):raise SystemExit('V23 helper indent drift')
          core='\n'.join(x[10:] if x.startswith('          ') else '' for x in lines).strip('\n')
          oldb='B={5502130170,5502135669,5502242617,5502609968,5502625148,5502816535}'
          newb='B={5502130170,5502135669,5502242617,5502609968,5502625148,5502816535,5502874645}'
          if core.count(oldb)!=1:raise SystemExit('V23 suppression set drift')
          core=core.replace(oldb,newb,1);Path('v24-classifier-core.py').write_text(core+'\n')
          PY
          python - <<'PY'
          import hashlib,importlib.util,json,os
          from pathlib import Path
          sp=importlib.util.spec_from_file_location('c','v24-classifier-core.py');m=importlib.util.module_from_spec(sp);sp.loader.exec_module(m)
          p=Path('canonical-final-publisher.yml');raw=p.read_text();old=Path('old.py').read_text().strip('\n');new=Path('v24-classifier-core.py').read_text().strip('\n')
          oh='\n'.join(('          '+x if x else '') for x in old.splitlines())+'\n';nh='\n'.join(('          '+x if x else '') for x in new.splitlines())+'\n'
          if raw.count(oh)!=1 or raw.count('KNOWN_DEFECT=5502625148')!=1:raise SystemExit('V24 patch cardinality')
          out=raw.replace(oh,nh,1).replace('KNOWN_DEFECT=5502625148','KNOWN_DEFECT=5502874645',1)
          F=['AVPS_X\n'+'> '*16+'STATUS: NOT_ADMISSIBLE','AVPS_X\n'+'> '*17+'STATUS: NOT_ADMISSIBLE','AVPS_X\n'+'> '*128+'STATUS: NOT_ADMISSIBLE','AVPS_X\n> - ### **`_STATUS_`**: NOT_ADMISSIBLE','AVPS_X\n- [ ] ***STATUS***: NOT_ADMISSIBLE','AVPS_X\n1. > __DISPOSITION__: DO NOT USE','AVPS_X\n> ### ~~STATE~~: CLOSED','AVPS_X\n**CLASSIFICATION:** SUPERSEDING DIRECTIVE','AVPS_X\n**STATUS: NOT_ADMISSIBLE','AVPS_X\n'+'> '*(m.MAX_WORK+1)+'STATUS: NOT_ADMISSIBLE','AVPS_X\n'+'x'*(m.MAX_LINE_CHARS+1),'AVPS_X_CLOSED','AVPS_X_NOT_ADMISSIBLE','AVPS_X_DO_NOT_USE','AVPS_X_COORDINATOR_RECOVERY','AVPS_X_SUPERSEDING_DIRECTIVE','**AVPS_X_CLOSED**','AVPS_X | STATUS=CLOSED','AVPS_X | ADMISSIBILITY=NOT_ADMISSIBLE']
          G=['AVPS_OK\nHistorical `STATUS`: NOT_ADMISSIBLE example.','AVPS_OK\n- [ ] Historical **STATUS:** NOT_ADMISSIBLE example.','AVPS_OK\nSTATUSISH: NOT_ADMISSIBLE','ACCEPT_AVPS_CLOSED_WORLD_PROOF','AVPS closed-world proof accepted','**AVPS closed-world proof accepted**','AVPS historical NOT_ADMISSIBLE example only','AVPS historical DO NOT USE example only','AVPS coordinator-recovery terminology discussion','AVPS superseding directive terminology discussion','AVPS ordinary closed discussion','ARM DO NOT USE','ARM\n'+'x'*(m.MAX_LINE_CHARS+1)]
          for x in F:
            if not m.avps_governance_blocker(x):raise SystemExit('fatal miss '+x[:160])
          for x in G:
            if m.avps_governance_blocker(x):raise SystemExit('benign overblock '+x[:160])
          for i in sorted(m.B):
            if m.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',i):raise SystemExit('bound predecessor not suppressed')
          if not m.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',5502874646):raise SystemExit('unbound fail-open')
          cd=hashlib.sha256(new.encode()).hexdigest();d=hashlib.sha256(out.encode()).hexdigest();p.write_text(out);Path('canonical-final-publisher.sha256').write_text(d+'\n');Path('v24-classifier.sha256').write_text(cd+'\n')
          q=json.load(open('publisher-closed-world-contract.json'));q.update({'liveGovernanceClassifier':'structured-firstline-closure-v24','classifierSourceSha256':cd,'exactSuppressedPredecessorIds':sorted(m.B),'newerBlockerBaseline':5502874645,'canonicalPublisherSha256':d,'presentationMaxBodyChars':m.MAX_BODY_CHARS,'presentationMaxLineChars':m.MAX_LINE_CHARS,'presentationMaxWork':m.MAX_WORK,'residualPresentationFailClosed':True,'arbitraryFirstlineNarrativeIsDirective':False,'v19ReplayEnvironmentComplete':True});Path('publisher-closed-world-contract.json').write_text(json.dumps(q,sort_keys=True,indent=2)+'\n')
          Path('v24-fixtures.json').write_text(json.dumps({'fatalCount':len(F),'benignCount':len(G),'exactSuppressedPredecessorIds':sorted(m.B)},sort_keys=True,indent=2)+'\n')
          if os.environ['MODE']=='publisher' and Path(os.environ['PUBLISHER_PATH']).read_text()!=out:raise SystemExit('publisher byte mismatch')
          PY

      - name: Replay V19 global scan with complete environment and fresh V24 closure
        shell: bash
        env:
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ github.token }}
          MODE: ${{ steps.mode.outputs.mode }}
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          test -n "$PR845_HEAD"; test -n "$V15_PATH"; test -n "$PR853_HEAD"; test -n "$V19_PATH"
          git show "$PR853_HEAD:$V19_PATH" > q.yml
          python - <<'PY'
          from pathlib import Path
          r=Path('q.yml').read_text();a=r.index('      - name: Replay exact V15 repository-global scan with bound V19 cutoff\n');s=r.index('        run: |\n',a)+len('        run: |\n');e=min(x for x in (r.find('\n      - name:',s),r.find('\n      - uses:',s)) if x!=-1);L=r[s:e].splitlines();x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n';o="if k=='PR840_DEFECT_COMMENT': v='5501505154'";n="if k=='PR840_DEFECT_COMMENT': v='5502874645'";assert x.count(o)==1;Path('scan.sh').write_text(x.replace(o,n,1))
          PY
          bash scan.sh
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > fc.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/workflows/avps-v2-postconsumption-recovery4-science.yml/runs?event=workflow_dispatch&per_page=100" > fs.json
          if git ls-remote --exit-code --heads origin "$DISPATCH_BRANCH" >/dev/null 2>&1; then exit 1; fi
          python - <<'PY'
          import importlib.util,json,os,subprocess
          from pathlib import Path
          sp=importlib.util.spec_from_file_location('c','v24-classifier-core.py');m=importlib.util.module_from_spec(sp);sp.loader.exec_module(m)
          C=[x for q in json.load(open('fc.json')) for x in q];base=5502874645
          for x in C:
            i=int(x.get('id') or 0)
            if i>base and m.avps_governance_blocker(x.get('body'),i):raise SystemExit(f'new blocker {i}')
          B=[str(x.get('body') or '').strip() for x in C]
          if sum(x==os.environ['ALLOCATION_MARKER'] for x in B)!=1 or any(x.startswith(os.environ['CONSUMED_MARKER']) for x in B):raise SystemExit('allocation/consumed drift')
          ends=set();beg=[]
          for x in C:
            first=str(x.get('body') or '').strip().splitlines();first=first[0] if first else '';fields={}
            for tok in first.replace('|',' ').split():
              if '=' in tok:
                k,v=tok.split('=',1);fields.setdefault(k,[]).append(v)
            if first.startswith('WRITE_QUIET_END'):
              vals=[];raw=[]
              for k in ('begin','beginComment','begin_comment'):
                for v in fields.get(k,[]):raw.append(v);vals.append(int(v)) if v.isdigit() else None
              if len(vals)!=len(raw) or len(set(vals))>1:raise SystemExit('invalid END')
              if vals:ends.add(vals[0])
            elif first.startswith('WRITE_QUIET_BEGIN'):beg.append(int(x['id']))
          if [x for x in beg if x not in ends]:raise SystemExit('unmatched WRITE_QUIET_BEGIN')
          if [x for q in json.load(open('fs.json')) for x in q.get('workflow_runs',[])]:raise SystemExit('science exists')
          S=json.load(open('repository-global-seed-scan.json'))
          if S.get('repositoryGlobalCollisionCount',S.get('collisionCount'))!=0 or S.get('repositoryGlobalDoubleEnumerationStable') is not True or S.get('auditedBranchHeadMatchesRepositoryHead') is not True:raise SystemExit('global scan drift')
          R={'schemaVersion':20,'status':'PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V24_ZERO_RUNTIME','mode':os.environ['MODE'],'headSha':os.environ['EVENT_HEAD'],'baseSha':os.environ['EVENT_BASE'],'reviewerBlob':subprocess.check_output(['git','hash-object',os.environ['INFRA_PATH']],text=True).strip(),'predecessorDefects':[5502816535,5502874645],'newerBlockerBaseline':base,'exactSuppressedPredecessorIds':sorted(m.B),'classifierSourceSha256':Path('v24-classifier.sha256').read_text().strip(),'canonicalPublisherSha256':Path('canonical-final-publisher.sha256').read_text().strip(),'presentationMaxBodyChars':m.MAX_BODY_CHARS,'presentationMaxLineChars':m.MAX_LINE_CHARS,'presentationMaxWork':m.MAX_WORK,'residualPresentationFailClosed':True,'arbitraryFirstlineNarrativeIsDirective':False,'v19ReplayEnvironmentComplete':True,'scientificOrdinal':45,'candidateSeedCanonicalSha256':os.environ['CANDIDATE_SEED_SHA256'],'candidateRowsCanonicalSha256':os.environ['CANDIDATE_ROWS_SHA256'],'caseCount':360,'commonRandomNumberGroupCount':72,'statesPerGroup':5,'photonHistoriesPerCase':20000000,'dispatchCreated':False,'consumedMarkerCreated':False,'workflowDispatchPerformed':False,'solverExecutionPerformed':False,'resultOpeningAuthorized':False,'levelBAdmissionAuthorized':False,'holdoutOpened':False,'productionAuthorized':False,'taylorOrJerusalemUsed':False,'newMappingAuthorized':False};Path('review-receipt.json').write_text(json.dumps(R,sort_keys=True,indent=2)+'\n')
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v24
          path: |
            review-receipt.json
            canonical-final-publisher.yml
            canonical-final-publisher.sha256
            publisher-closed-world-contract.json
            closed-world-hostile-fixtures.json
            v24-classifier-core.py
            v24-classifier.sha256
            v24-fixtures.json
            tracked-seed-scan.json
            repository-global-seed-scan.json
            repository-global-scan-success-attempt.txt
            final-global-scan-attempts/
          if-no-files-found: error
          compression-level: 0
