name: Review AVPS Recovery4 ordinal45 final dispatch choreography v26

on:
  pull_request:
    types: [opened]
    branches: [main]
    paths:
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v26.yml
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml

permissions: {contents: read, actions: read, issues: read, pull-requests: read}
concurrency:
  group: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v26-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      INFRA_BASE: de3fafa052ff1d5b2f33ec0d4347c884d560d8b6
      INFRA_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v26
      INFRA_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v26.yml
      PUBLISHER_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1
      PUBLISHER_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml
      PR845_HEAD: e461c26ec78e9832ff43859bf63baf49464628b1
      V15_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v1.yml
      PR853_HEAD: 3a2ee4d92d634aec7575f813dea5adf38ba18400
      V19_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v19.yml
      PR863_HEAD: d5cc6a549403b2bdfe42512fc9c37e4de1fa3f25
      PR863_BLOB: 48eb8bd0ea7ac2082beaceb3831b2785d601bc8c
      PR863_ARTIFACT: '9827324743'
      PR863_ARTIFACT_NAME: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v22
      PR863_ARTIFACT_DIGEST: sha256:9f31e55472fec84f4293e71158eb0a6db076e453567786a41483c27825045e4a
      PR864_HEAD: 1d4bf9dae0edb9969ef05ffa1584cf6cb3eba3eb
      V23_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v23.yml
      PR865_HEAD: a36cc4793df092aa883fd7a9f627f6fc02c38335
      PR865_TREE: 02e26d62e289ed378cc4c243c34bc3ca34852097
      PR865_BLOB: 6f02455e90a7040a7ae1b17b7a4c28a1111b5835
      PR865_RUN: '33578940599'
      PR865_JOB: '100088918674'
      PR865_GENERIC: '33578940669'
      PR865_GENERIC_JOB: '100088918779'
      V24_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v24.yml
      V23_COORD_DEFECT: '5502874645'
      V23_BACKUP_DEFECT: '5502893055'
      V24_DEFECT: '5502911050'
      PR866_HEAD: 6352d9294264ce198bb321dd7fb6902ce399966d
      PR866_TREE: da5e11e03640ec9c7d21e34adb604dd73bd6ae9e
      PR866_BLOB: e8ff97de40f416a4678d55d5bdfd7547955479a5
      PR866_RUN: '33579241722'
      PR866_JOB: '100089799118'
      PR866_GENERIC: '33579241674'
      PR866_GENERIC_JOB: '100089799273'
      PR866_ARTIFACT: '9827926869'
      PR866_ARTIFACT_NAME: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v25
      PR866_ARTIFACT_DIGEST: sha256:44833c1aadfcc971a0369ba5e66a3c03580fa8770a886fc0fc79a8a9d808d539
      V25_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v25.yml
      V25_PRIMARY_DEFECT: '5503344359'
      V25_BACKUP_DEFECT: '5503366764'
      ALLOCATION_MARKER: ORDINAL45_AVPS_V2_POSTCONSUMPTION_RECOVERY4_AUTHORIZATION_ALLOCATED_REVIEWED_NOT_DISPATCHED commit=6e095b4b1603c90dcee0943295909b30cd1b374d parent=5685d5c313071014a94c816ee26b129f1f17a7fc pr=774
      CONSUMED_MARKER: ORDINAL45_AVPS_V2_POSTCONSUMPTION_RECOVERY4_DISPATCH_CONSUMED
      DISPATCH_BRANCH: dispatch/aerosol-vertical-profile-sensitivity-v2-postconsumption-recovery4-ordinal-45
      SCIENCE_WORKFLOW: .github/workflows/avps-v2-postconsumption-recovery4-science.yml
      SCIENCE_WORKFLOW_BLOB: 2d210b3a161f6889352d600fb9222c974e728034
      RUNTIME_ADAPTER: runtime-avps-v2-recovery4-ordinal45-v1/runtime_adapter.py
      RUNTIME_ADAPTER_BLOB: e4b690c2969fd01ecde5b08f554e47ad50b40914
      EXECUTOR: runtime-avps-v2-recovery4-ordinal45-v1/executor.py
      EXECUTOR_BLOB: 7dc38e31c19701b1a1ceef300d75105cff179290
      AGGREGATOR: runtime-avps-v2-recovery4-ordinal45-v1/aggregator.py
      AGGREGATOR_BLOB: 6afc8b0d21c2b5553c251dc9ca5524c795ed2646
      RUNTIME_CONTRACT: runtime-avps-v2-recovery4-ordinal45-v1/runtime_contract.json
      RUNTIME_CONTRACT_BLOB: 2d5f71d80b5e573b86775c2806b80dcc1fbe02f4
      CANDIDATE_SEED_SHA256: ddded6b2d170ca2fac8d498bdba2887446c16995df0880d948fb2be00870b3de
      CANDIDATE_ROWS_SHA256: c439de417520b330c037e2628df02b6955f652563300aa5ef30477abf7661a98
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: '3.13'}

      - name: Bind V26 identity, rejected V25 evidence, and exact-only live fence
        id: mode
        shell: bash
        env:
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = pull_request; test "${{ github.event.action }}" = opened; test "$GITHUB_RUN_ATTEMPT" = 1
          test "$(git rev-parse HEAD)" = "$EVENT_HEAD"; test "$(git rev-parse HEAD^)" = "$EVENT_BASE"; test "$(git rev-list --count "$EVENT_BASE"..HEAD)" = 1
          mapfile -t C < <(git diff --name-only "$EVENT_BASE"...HEAD); test "${#C[@]}" = 1
          if [ "${C[0]}" = "$INFRA_PATH" ]; then
            test "$EVENT_BASE" = "$INFRA_BASE"; test "$EVENT_BRANCH" = "$INFRA_BRANCH"; test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$INFRA_BASE"; echo mode=infra >> "$GITHUB_OUTPUT"
          elif [ "${C[0]}" = "$PUBLISHER_PATH" ]; then
            test "$EVENT_BRANCH" = "$PUBLISHER_BRANCH"; test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$EVENT_BASE"; test "$(git rev-parse "$EVENT_BASE:$INFRA_PATH")" = "$(git rev-parse "HEAD:$INFRA_PATH")"; echo mode=publisher >> "$GITHUB_OUTPUT"
          else exit 1; fi
          git fetch --no-tags origin "$PR866_HEAD" "$PR865_HEAD" "$PR864_HEAD" "$PR863_HEAD" "$PR853_HEAD" "$PR845_HEAD"
          test "$(git rev-parse "$PR866_HEAD^{tree}")" = "$PR866_TREE"; test "$(git rev-parse "$PR866_HEAD:$V25_PATH")" = "$PR866_BLOB"
          if git ls-remote --exit-code --heads origin "$DISPATCH_BRANCH" >/dev/null 2>&1; then exit 1; fi
          gh api "repos/$GITHUB_REPOSITORY/pulls/866" > p.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR866_RUN" > r.json
          gh api "repos/$GITHUB_REPOSITORY/actions/jobs/$PR866_JOB" > j.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR866_GENERIC" > g.json
          gh api "repos/$GITHUB_REPOSITORY/actions/jobs/$PR866_GENERIC_JOB" > gj.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR866_RUN/artifacts?per_page=100" > a.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > c.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/workflows/avps-v2-postconsumption-recovery4-science.yml/runs?event=workflow_dispatch&per_page=100" > s.json
          python - <<'PY'
          import json,os
          z=lambda p:json.load(open(p));p,r,j,g,gj=z('p.json'),z('r.json'),z('j.json'),z('g.json'),z('gj.json')
          if p.get('state')!='closed' or p.get('merged_at') is not None or p['head']['sha']!=os.environ['PR866_HEAD'] or p['base']['sha']!=os.environ['INFRA_BASE']:raise SystemExit('PR866 drift')
          if r['id']!=int(os.environ['PR866_RUN']) or r['head_sha']!=os.environ['PR866_HEAD'] or r['run_attempt']!=1 or r['conclusion']!='success':raise SystemExit('V25 dedicated drift')
          if j['id']!=int(os.environ['PR866_JOB']) or j['run_id']!=r['id'] or j['conclusion']!='success':raise SystemExit('V25 job drift')
          st={x.get('name'):x.get('conclusion') for x in j.get('steps',[])}
          exp={'Bind V25 identity, rejected V24 and complete V23 predecessor ledger':'success','Run actions/download-artifact@v4':'success','Replay exact V24 build and bind complete V25 predecessor ledger':'success','Replay exact V24 global scan with bound V25 cutoff':'success','Run actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a':'success'}
          for k,v in exp.items():
            if st.get(k)!=v:raise SystemExit('V25 step drift '+k)
          if g['id']!=int(os.environ['PR866_GENERIC']) or g['head_sha']!=os.environ['PR866_HEAD'] or g['run_attempt']!=1 or g['conclusion']!='success':raise SystemExit('V25 generic run drift')
          if gj['id']!=int(os.environ['PR866_GENERIC_JOB']) or gj['run_id']!=g['id'] or gj['conclusion']!='success':raise SystemExit('V25 generic job drift')
          A=[x for x in z('a.json').get('artifacts',[]) if int(x.get('id') or 0)==int(os.environ['PR866_ARTIFACT'])]
          if len(A)!=1 or A[0]['name']!=os.environ['PR866_ARTIFACT_NAME'] or A[0].get('digest')!=os.environ['PR866_ARTIFACT_DIGEST'] or A[0].get('expired'):raise SystemExit('V25 artifact drift')
          C=[x for q in z('c.json') for x in q]
          defs=[(os.environ['V25_PRIMARY_DEFECT'],'INDEPENDENT_AUDIT::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V25_PR866_PUBLISHER_CASE_SENSITIVE_AVPS_OUTER_GATE_BYPASS'),(os.environ['V25_BACKUP_DEFECT'],'BACKUP_INDEPENDENT_AUDIT::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V25_PR866_PUBLISHER_CASE_SENSITIVE_AVPS_OUTER_GATE_BYPASS')]
          for cid,pref in defs:
            h=[x for x in C if int(x.get('id') or 0)==int(cid)]
            if len(h)!=1 or not str(h[0].get('body') or '').startswith(pref):raise SystemExit('defect binding '+cid)
            u=h[0].get('user') or {}
            if u.get('login')!='search-maker' or int(u.get('id') or 0)!=304153003 or h[0].get('author_association')!='OWNER' or 'QUARANTINE_DOWNSTREAM=false' not in str(h[0].get('body') or ''):raise SystemExit('defect identity '+cid)
          base=int(os.environ['V25_BACKUP_DEFECT']); fatal=('CONFIRMED_DEFECT','MATERIAL_RISK','QUARANTINE_DOWNSTREAM=true','FAIL_CLOSED','FAIL-CLOSED','BLOCKER','NOT_ADMISSIBLE','DO NOT USE')
          for x in C:
            if int(x.get('id') or 0)>base and 'AVPS' in str(x.get('body') or '').upper() and any(t.upper() in str(x.get('body') or '').upper() for t in fatal):raise SystemExit('newer unbound AVPS blocker '+str(x.get('id')))
          bodies=[str(x.get('body') or '').strip() for x in C]
          if sum(x==os.environ['ALLOCATION_MARKER'] for x in bodies)!=1 or any(x.startswith(os.environ['CONSUMED_MARKER']) for x in bodies):raise SystemExit('allocation/consumed drift')
          ends=set();beg=[]
          for x in C:
            first=str(x.get('body') or '').strip().splitlines();first=first[0] if first else '';fields={}
            for tok in first.replace('|',' ').split():
              if '=' in tok:
                k,v=tok.split('=',1);fields.setdefault(k,[]).append(v)
            if first.startswith('WRITE_QUIET_END'):
              vals=[];raw=[]
              for k in ('begin','beginComment','begin_comment'):
                for v in fields.get(k,[]):raw.append(v);vals.append(int(v)) if v.isdigit() else None
              if len(vals)!=len(raw) or len(set(vals))>1:raise SystemExit('invalid END')
              if vals:ends.add(vals[0])
            elif first.startswith('WRITE_QUIET_BEGIN'):beg.append(int(x['id']))
          if [x for x in beg if x not in ends]:raise SystemExit('unmatched fence')
          if [x for q in z('s.json') for x in q.get('workflow_runs',[])]:raise SystemExit('science exists')
          PY
          test "$(git rev-parse "HEAD:$SCIENCE_WORKFLOW")" = "$SCIENCE_WORKFLOW_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_ADAPTER")" = "$RUNTIME_ADAPTER_BLOB"
          test "$(git rev-parse "HEAD:$EXECUTOR")" = "$EXECUTOR_BLOB"
          test "$(git rev-parse "HEAD:$AGGREGATOR")" = "$AGGREGATOR_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_CONTRACT")" = "$RUNTIME_CONTRACT_BLOB"

      - uses: actions/download-artifact@v4
        with: {name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v25, path: v25, repository: '${{ github.repository }}', run-id: 33579241722, github-token: '${{ github.token }}'}

      - name: Build exact V26 publisher and case-insensitive state_check fixtures
        shell: bash
        env: {MODE: '${{ steps.mode.outputs.mode }}'}
        run: |
          set -euo pipefail
          cp -a v25/. .
          test "$(cat canonical-final-publisher.sha256)" = "a9664b7a81b04aba848910b146121baf2268c42dfb078141df6022a79c9c6baf"
          test "$(cat v25-classifier.sha256)" = "3855674d3c188f7f04b9c2a73c3b85ea2192d580455e4543e35bf73a58b59310"
          python - <<'PY'
          import hashlib,importlib.util,json,os
          from pathlib import Path
          p=Path('canonical-final-publisher.yml');raw=p.read_text();old=Path('v25-classifier-core.py').read_text().strip('\n')
          oldb='B={5502130170,5502135669,5502242617,5502609968,5502625148,5502816535,5502874645,5502893055,5502911050}'
          newb='B={5502130170,5502135669,5502242617,5502609968,5502625148,5502816535,5502874645,5502893055,5502911050,5503344359,5503366764}'
          if old.count(oldb)!=1:raise SystemExit('V25 helper ledger drift')
          new=old.replace(oldb,newb,1)
          oh='\n'.join(('          '+x if x else '') for x in old.splitlines())+'\n';nh='\n'.join(('          '+x if x else '') for x in new.splitlines())+'\n'
          oldgate="if 'AVPS' in b and avps_governance_blocker(b,int(c.get('id') or 0)): raise SystemExit(f'newer AVPS blocker/defect exists: {c.get(\"id\")}')"
          newgate="if avps_governance_blocker(b,int(c.get('id') or 0)): raise SystemExit(f'newer AVPS blocker/defect exists: {c.get(\"id\")}')"
          if raw.count(oh)!=1 or raw.count('KNOWN_DEFECT=5502911050')!=1 or raw.count(oldgate)!=1:raise SystemExit('V26 canonical patch drift')
          out=raw.replace(oh,nh,1).replace('KNOWN_DEFECT=5502911050','KNOWN_DEFECT=5503366764',1).replace(oldgate,newgate,1)
          if oldgate in out or out.count(newgate)!=1:raise SystemExit('publisher outer-gate correction drift')
          p.write_text(out)
          for q in ('v24-classifier-core.py','v25-classifier-core.py','v26-classifier-core.py'):Path(q).write_text(new+'\n')
          cd=hashlib.sha256(new.encode()).hexdigest();d=hashlib.sha256(out.encode()).hexdigest();Path('canonical-final-publisher.sha256').write_text(d+'\n');Path('v25-classifier.sha256').write_text(cd+'\n');Path('v26-classifier.sha256').write_text(cd+'\n')
          sp=importlib.util.spec_from_file_location('c','v26-classifier-core.py');m=importlib.util.module_from_spec(sp);sp.loader.exec_module(m)
          for i in (5503344359,5503366764):
            if m.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',i):raise SystemExit('bound V25 predecessor not suppressed')
          if not m.avps_governance_blocker('Confirmed_Defect avps_x',5503366765):raise SystemExit('case-insensitive helper fail-open')
          marker='# BEGIN_CANONICAL_AVPS_ORDINAL45_PREFENCE_V1\n';a=out.index(marker);call='          state_check()\n';e=out.index(call,a)
          block=out[a:e]
          lines=block.splitlines();code='\n'.join((x[10:] if x.startswith('          ') else x) for x in lines)+'\n'
          ns={};exec(compile(code,'canonical-state-check-fixture','exec'),ns)
          class CP:
            returncode=2;stderr='';stdout=''
          ns['subprocess'].run=lambda *a,**k: CP()
          alloc={'id':1,'body':ns['ALLOCATION_MARKER']}
          def check(body,should_block):
            comment={'id':5503366765,'body':body}
            ns['gh_pages']=lambda endpoint:[[alloc,comment]] if '/issues/60/comments' in endpoint else [{'workflow_runs':[]}]
            blocked=False
            try: ns['state_check']()
            except SystemExit as ex:
              if 'newer AVPS blocker/defect exists' not in str(ex):raise
              blocked=True
            if blocked!=should_block:raise SystemExit('publisher state_check case fixture mismatch '+repr(body))
          for body in ('avps_ordinal45_transition\nSTATUS: NOT_ADMISSIBLE','AvPs_ordinal45_transition\nSTATUS: NOT_ADMISSIBLE','Independent_Audit::Confirmed_Defect avps_ordinal45_synthetic','AVPS_ordinal45_transition\nSTATUS: NOT_ADMISSIBLE'):check(body,True)
          check('avps_ordinal45_acceptance\nSTATUS: ACCEPTED',False)
          q=json.load(open('publisher-closed-world-contract.json'));q.update({'liveGovernanceClassifier':'structured-firstline-closure-v26','classifierSourceSha256':cd,'exactSuppressedPredecessorIds':sorted(m.B),'newerBlockerBaseline':5503366764,'canonicalPublisherSha256':d,'publisherOuterAvpsGateCaseInsensitiveAligned':True,'reviewerAndPublisherClassifierSemanticsAligned':True,'publisherStateCheckCaseFixturesPassed':True});Path('publisher-closed-world-contract.json').write_text(json.dumps(q,sort_keys=True,indent=2)+'\n')
          if os.environ['MODE']=='publisher' and Path(os.environ['PUBLISHER_PATH']).read_text()!=out:raise SystemExit('publisher byte mismatch')
          PY

      - name: Replay exact V25 global scan with bound V26 cutoff
        shell: bash
        env:
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ github.token }}
          MODE: ${{ steps.mode.outputs.mode }}
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          git show "$PR866_HEAD:$V25_PATH" > v25.yml
          python - <<'PY'
          from pathlib import Path
          raw=Path('v25.yml').read_text();name='Replay exact V24 global scan with bound V25 cutoff';a=raw.index(f'      - name: {name}\n');s=raw.index('        run: |\n',a)+len('        run: |\n');e=min(x for x in (raw.find('\n      - name:',s),raw.find('\n      - uses:',s)) if x!=-1);L=raw[s:e].splitlines();assert all((not x) or x.startswith('          ') for x in L);x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n';count=x.count('5502911050')
          if count<2:raise SystemExit('V25 cutoff occurrences drift')
          Path('v26-scan.sh').write_text(x.replace('5502911050','5503366764'))
          PY
          test -n "$PR845_HEAD"; test -n "$V15_PATH"; test -n "$PR853_HEAD"; test -n "$V19_PATH"
          bash v26-scan.sh
          python - <<'PY'
          import json,os,subprocess
          from pathlib import Path
          r=json.load(open('review-receipt.json'));r.update({'schemaVersion':22,'status':'PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V26_ZERO_RUNTIME','headSha':os.environ['EVENT_HEAD'],'baseSha':os.environ['EVENT_BASE'],'reviewerBlob':subprocess.check_output(['git','hash-object',os.environ['INFRA_PATH']],text=True).strip(),'predecessorDefects':[5503344359,5503366764],'newerBlockerBaseline':5503366764,'classifierSourceSha256':Path('v26-classifier.sha256').read_text().strip(),'canonicalPublisherSha256':Path('canonical-final-publisher.sha256').read_text().strip(),'publisherOuterAvpsGateCaseInsensitiveAligned':True,'publisherStateCheckCaseFixturesPassed':True,'reviewerAndPublisherClassifierSemanticsAligned':True});Path('review-receipt.json').write_text(json.dumps(r,sort_keys=True,indent=2)+'\n')
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v26
          path: |
            review-receipt.json
            canonical-final-publisher.yml
            canonical-final-publisher.sha256
            publisher-closed-world-contract.json
            closed-world-hostile-fixtures.json
            v26-classifier-core.py
            v26-classifier.sha256
            v24-fixtures.json
            tracked-seed-scan.json
            repository-global-seed-scan.json
            repository-global-scan-success-attempt.txt
            final-global-scan-attempts/
          if-no-files-found: error
          compression-level: 0
