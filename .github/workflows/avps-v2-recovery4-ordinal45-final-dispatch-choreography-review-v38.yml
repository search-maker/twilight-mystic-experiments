name: Review AVPS Recovery4 ordinal45 final dispatch choreography v38

on:
  pull_request:
    types: [opened]
    branches: [main]
    paths:
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml

permissions: {contents: read, actions: read, issues: read, pull-requests: read}
concurrency:
  group: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      INFRA_BASE: de3fafa052ff1d5b2f33ec0d4347c884d560d8b6
      INFRA_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v38
      INFRA_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml
      PUBLISHER_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1
      PUBLISHER_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml
      PR890_HEAD: 21d575825036b29840c7e021047f68e2fe6a0729
      PR890_TREE: 906ed4d00bf25582407381998f397d94b6d2e9e4
      PR890_BLOB: 315a20c8a4e8dd9414b954872d6d51eb97a4829f
      PR890_RUN: '33629118640'
      PR890_JOB: '100243891236'
      PR890_GENERIC: '33629118546'
      PR890_GENERIC_JOB: '100243890876'
      V37_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v37.yml
      V37_DEFECT: '5510140594'
      V36_DEFECT: '5509304642'
      V35_DEFECT: '5508214692'
      V35_RISK: '5508235083'
      LIVE_CUTOFF: '5510140594'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: '3.13'}

      - name: Bind V38 identity, rejected V37 evidence, nested static env, and exact-only live fence
        id: mode
        shell: bash
        env:
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$GITHUB_EVENT_NAME" = pull_request
          test "${{ github.event.action }}" = opened
          test "$GITHUB_RUN_ATTEMPT" = 1
          test "$(git rev-parse HEAD)" = "$EVENT_HEAD"
          test "$(git rev-parse HEAD^)" = "$EVENT_BASE"
          test "$(git rev-list --count "$EVENT_BASE"..HEAD)" = 1
          mapfile -t C < <(git diff --name-only "$EVENT_BASE"...HEAD)
          test "${#C[@]}" = 1
          if [ "${C[0]}" = "$INFRA_PATH" ]; then
            test "$EVENT_BASE" = "$INFRA_BASE"
            test "$EVENT_BRANCH" = "$INFRA_BRANCH"
            test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$INFRA_BASE"
            echo mode=infra >> "$GITHUB_OUTPUT"
          elif [ "${C[0]}" = "$PUBLISHER_PATH" ]; then
            test "$EVENT_BRANCH" = "$PUBLISHER_BRANCH"
            test "$(git ls-remote origin refs/heads/main|cut -f1)" = "$EVENT_BASE"
            test "$(git rev-parse "$EVENT_BASE:$INFRA_PATH")" = "$(git rev-parse "HEAD:$INFRA_PATH")"
            echo mode=publisher >> "$GITHUB_OUTPUT"
          else
            exit 1
          fi

          git fetch --no-tags origin "$PR890_HEAD"
          test "$(git rev-parse "$PR890_HEAD^{tree}")" = "$PR890_TREE"
          test "$(git rev-parse "$PR890_HEAD:$V37_PATH")" = "$PR890_BLOB"
          git show "$PR890_HEAD:$V37_PATH" > v37.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst,skip={'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in skip: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v37.yml','v37-inherited-env.sh')
          PY
          source v37-inherited-env.sh
          git fetch --no-tags origin "$PR889_HEAD"
          test "$(git rev-parse "$PR889_HEAD:$V36_PATH")" = "$PR889_BLOB"
          git show "$PR889_HEAD:$V36_PATH" > v36.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v36.yml','v36-inherited-env.sh')
          PY
          source v36-inherited-env.sh
          git fetch --no-tags origin "$PR888_HEAD"
          test "$(git rev-parse "$PR888_HEAD:$V35_PATH")" = "$PR888_BLOB"
          git show "$PR888_HEAD:$V35_PATH" > v35.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          lines=Path('v35.yml').read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
          for line in lines[a:e]:
              if not line.startswith('      ') or ':' not in line: continue
              k,v=line.strip().split(':',1); v=v.strip()
              if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
              if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
              out.append('export '+k+'='+shlex.quote(v))
          Path('v35-inherited-env.sh').write_text('\n'.join(out)+'\n')
          PY
          source v35-inherited-env.sh
          git fetch --no-tags origin "$PR886_HEAD"
          test "$(git rev-parse "$PR886_HEAD:$V32_PATH")" = "$PR886_BLOB"
          git show "$PR886_HEAD:$V32_PATH" > v32.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          lines=Path('v32.yml').read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
          for line in lines[a:e]:
              if not line.startswith('      ') or ':' not in line: continue
              k,v=line.strip().split(':',1); v=v.strip()
              if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
              if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
              out.append('export '+k+'='+shlex.quote(v))
          Path('v32-static-env.sh').write_text('\n'.join(out)+'\n')
          PY
          source v32-static-env.sh
          export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v38"
          export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml"
          export LIVE_CUTOFF='5510140594'
          export CURRENT_LIVE_CUTOFF="$LIVE_CUTOFF"
          test -n "$ALLOCATION_MARKER"
          test -n "$CONSUMED_MARKER"
          test -n "$SCIENCE_WORKFLOW"
          test -n "$RUNTIME_ADAPTER"
          if git ls-remote --exit-code --heads origin "$DISPATCH_BRANCH" >/dev/null 2>&1; then exit 1; fi

          gh api "repos/$GITHUB_REPOSITORY/pulls/890" > p37.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR890_RUN" > r37.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR890_RUN/jobs?per_page=100" > j37s.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR890_GENERIC" > g37.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR890_GENERIC/jobs?per_page=100" > gj37s.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR890_RUN/artifacts?per_page=100" > a37.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > c.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/actions/workflows/avps-v2-postconsumption-recovery4-science.yml/runs?event=workflow_dispatch&per_page=100" > s.json
          python - <<'PY'
          import json,os
          z=lambda p:json.load(open(p)); p,r,g=z('p37.json'),z('r37.json'),z('g37.json')
          js=z('j37s.json').get('jobs',[]); gjs=z('gj37s.json').get('jobs',[])
          if p.get('state')!='closed' or p.get('merged_at') is not None or p['head']['sha']!=os.environ['PR890_HEAD'] or p['base']['sha']!=os.environ['INFRA_BASE']: raise SystemExit('PR890 drift')
          if r['id']!=int(os.environ['PR890_RUN']) or r['head_sha']!=os.environ['PR890_HEAD'] or r['run_attempt']!=1 or r['conclusion']!='failure': raise SystemExit('V37 dedicated drift')
          hits=[x for x in js if int(x.get('id') or 0)==int(os.environ['PR890_JOB'])]
          if len(hits)!=1 or hits[0].get('conclusion')!='failure': raise SystemExit('V37 job drift')
          j=hits[0]; st={x.get('name'):x.get('conclusion') for x in j.get('steps',[])}
          exp={'Bind V37 identity, rejected V36 evidence, nested V32 static env, and exact-only live fence':'success','Run actions/download-artifact@v4':'success','Replay exact V36 build and bind V37 predecessor ledger':'failure','Replay exact V36 global scan with preserved V36 replay-environment repairs':'skipped','Run actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a':'skipped'}
          for k,v in exp.items():
              if st.get(k)!=v: raise SystemExit('V37 step drift '+k)
          if g['id']!=int(os.environ['PR890_GENERIC']) or g['head_sha']!=os.environ['PR890_HEAD'] or g['run_attempt']!=1 or g['conclusion']!='success': raise SystemExit('V37 generic drift')
          gh=[x for x in gjs if int(x.get('id') or 0)==int(os.environ['PR890_GENERIC_JOB'])]
          if len(gh)!=1 or gh[0].get('conclusion')!='success': raise SystemExit('V37 generic job drift')
          if z('a37.json').get('artifacts'): raise SystemExit('V37 unexpected artifact')
          C=[x for q in z('c.json') for x in q]
          expected={
            int(os.environ['V35_DEFECT']):'COORDINATOR::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V35_PR888_REPOSITORY_GLOBAL_SCAN_STALE_PREEXISTING_FILE_PRECONDITION_ATTEMPT1_FAILURE',
            int(os.environ['V35_RISK']):'COORDINATOR::MATERIAL_RISK AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V35_HISTORICAL_ENV_SOURCE_OVERWRITES_LIVE_CUTOFF',
            int(os.environ['V36_DEFECT']):'COORDINATOR::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V36_PR889_BIND_ENV_MISSING_NESTED_V32_STATIC_ENV_ATTEMPT1_FAILURE',
            int(os.environ['V37_DEFECT']):'COORDINATOR::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V37_PR890_BUILD_STEP_ENV_NOT_PERSISTED_ATTEMPT1_FAILURE',
          }
          for cid,pref in expected.items():
              hits=[x for x in C if int(x.get('id') or 0)==cid]
              if len(hits)!=1 or not str(hits[0].get('body') or '').startswith(pref): raise SystemExit('predecessor decision binding '+str(cid))
              u=hits[0].get('user') or {}; b=str(hits[0].get('body') or '')
              if u.get('login')!='search-maker' or int(u.get('id') or 0)!=304153003 or hits[0].get('author_association')!='OWNER' or 'QUARANTINE_DOWNSTREAM=false' not in b: raise SystemExit('predecessor identity '+str(cid))
          base=int(os.environ['LIVE_CUTOFF']); fatal=('CONFIRMED_DEFECT','MATERIAL_RISK','QUARANTINE_DOWNSTREAM=true','FAIL_CLOSED','FAIL-CLOSED','BLOCKER','NOT_ADMISSIBLE','DO NOT USE')
          for x in C:
              b=str(x.get('body') or '')
              if int(x.get('id') or 0)>base and 'AVPS' in b.upper() and any(t.upper() in b.upper() for t in fatal): raise SystemExit('newer unbound AVPS blocker '+str(x.get('id')))
          bodies=[str(x.get('body') or '').strip() for x in C]
          if sum(x==os.environ['ALLOCATION_MARKER'] for x in bodies)!=1 or any(x.startswith(os.environ['CONSUMED_MARKER']) for x in bodies): raise SystemExit('allocation/consumed drift')
          ends=set(); beg=[]
          for x in C:
              first=str(x.get('body') or '').strip().splitlines(); first=first[0] if first else ''; fields={}
              for tok in first.replace('|',' ').split():
                  if '=' in tok:
                      k,v=tok.split('=',1); fields.setdefault(k,[]).append(v)
              if first.startswith('WRITE_QUIET_END'):
                  vals=[]; raw=[]
                  for k in ('begin','beginComment','begin_comment'):
                      for v in fields.get(k,[]): raw.append(v); vals.append(int(v)) if v.isdigit() else None
                  if len(vals)!=len(raw) or len(set(vals))>1: raise SystemExit('invalid END')
                  if vals: ends.add(vals[0])
              elif first.startswith('WRITE_QUIET_BEGIN'): beg.append(int(x['id']))
          if [x for x in beg if x not in ends]: raise SystemExit('unmatched fence')
          if [x for q in z('s.json') for x in q.get('workflow_runs',[])]: raise SystemExit('science exists')
          PY
          test "$(git rev-parse "HEAD:$SCIENCE_WORKFLOW")" = "$SCIENCE_WORKFLOW_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_ADAPTER")" = "$RUNTIME_ADAPTER_BLOB"
          test "$(git rev-parse "HEAD:$EXECUTOR")" = "$EXECUTOR_BLOB"
          test "$(git rev-parse "HEAD:$AGGREGATOR")" = "$AGGREGATOR_BLOB"
          test "$(git rev-parse "HEAD:$RUNTIME_CONTRACT")" = "$RUNTIME_CONTRACT_BLOB"

      - uses: actions/download-artifact@v4
        with: {name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v25, path: v25, repository: '${{ github.repository }}', run-id: 33579241722, github-token: '${{ github.token }}'}

      - name: Replay exact V37 build with per-step historical environment and bind V38 predecessor ledger
        shell: bash
        env: {MODE: '${{ steps.mode.outputs.mode }}'}
        run: |
          set -euo pipefail
          git show "$PR890_HEAD:$V37_PATH" > v37.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v37.yml','v37-step-env.sh')
          PY
          source v37-step-env.sh
          git show "$PR889_HEAD:$V36_PATH" > v36.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v36.yml','v36-step-env.sh')
          PY
          source v36-step-env.sh
          git show "$PR888_HEAD:$V35_PATH" > v35.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v35.yml','v35-step-env.sh')
          PY
          source v35-step-env.sh
          git show "$PR886_HEAD:$V32_PATH" > v32.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v32.yml','v32-step-env.sh')
          PY
          source v32-step-env.sh
          export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v38"
          export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml"
          export LIVE_CUTOFF='5510140594'
          export CURRENT_LIVE_CUTOFF="$LIVE_CUTOFF"
          test -n "$PR888_HEAD"; test -n "$ALLOCATION_MARKER"; test "$LIVE_CUTOFF" = "$CURRENT_LIVE_CUTOFF"
          python - <<'PY'
          from pathlib import Path
          raw=Path('v37.yml').read_text(); name='Replay exact V36 build and bind V37 predecessor ledger'; a=raw.index(f'      - name: {name}\n'); s=raw.index('        run: |\n',a)+len('        run: |\n'); e=min(x for x in (raw.find('\n      - name:',s),raw.find('\n      - uses:',s)) if x!=-1); L=raw[s:e].splitlines(); assert all((not x) or x.startswith('          ') for x in L); x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n'
          repl=(
            ('export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v37"','export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v38"',1),
            ('export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v37.yml"','export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml"',1),
            ("'5509304642'","'5510140594'",2),
          )
          for old,new,n in repl:
              if x.count(old)!=n: raise SystemExit('V37 build patch cardinality drift '+old+' '+str(x.count(old)))
              x=x.replace(old,new)
          Path('v37-build.sh').write_text(x)
          PY
          bash v37-build.sh
          export LIVE_CUTOFF="$CURRENT_LIVE_CUTOFF"
          test "$LIVE_CUTOFF" = '5510140594'
          python - <<'PY'
          import hashlib,importlib.util,json,os,re
          from pathlib import Path
          p=Path('canonical-final-publisher.yml'); raw=p.read_text(); old=Path('v37-classifier-core.py').read_text().strip('\n')
          m=re.findall(r'^B=\{([0-9,]+)\}$',old,re.M)
          if len(m)!=1: raise SystemExit('V37 helper ledger shape drift')
          vals=[int(x) for x in m[0].split(',') if x]; did=int(os.environ['V37_DEFECT'])
          if int(os.environ['V36_DEFECT']) not in vals or int(os.environ['V35_DEFECT']) not in vals or int(os.environ['V35_RISK']) not in vals or did in vals: raise SystemExit('V37 helper ledger content drift')
          newvals=vals+[did]; oldb='B={'+m[0]+'}'; newb='B={'+','.join(map(str,newvals))+'}'; new=old.replace(oldb,newb,1)
          oh='\n'.join(('          '+x if x else '') for x in old.splitlines())+'\n'; nh='\n'.join(('          '+x if x else '') for x in new.splitlines())+'\n'
          if raw.count(oh)!=1: raise SystemExit('V38 embedded helper patch drift')
          out=raw.replace(oh,nh,1); p.write_text(out)
          Path('v38-classifier-core.py').write_text(new+'\n'); cd=hashlib.sha256(new.encode()).hexdigest(); d=hashlib.sha256(out.encode()).hexdigest(); Path('v38-classifier.sha256').write_text(cd+'\n'); Path('canonical-final-publisher.sha256').write_text(d+'\n')
          sp=importlib.util.spec_from_file_location('c','v38-classifier-core.py'); mod=importlib.util.module_from_spec(sp); sp.loader.exec_module(mod)
          if mod.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',did): raise SystemExit('V37 defect not suppressed')
          if not mod.avps_governance_blocker('confirmed_defect avps_x',int(os.environ['LIVE_CUTOFF'])+1): raise SystemExit('fresh blocker fail-open')
          q=json.load(open('publisher-closed-world-contract.json')); q.update({'liveGovernanceClassifier':'structured-firstline-closure-v38','classifierSourceSha256':cd,'exactSuppressedPredecessorIds':sorted(mod.B),'newerBlockerBaseline':int(os.environ['LIVE_CUTOFF']),'canonicalPublisherSha256':d,'v37PerStepHistoricalEnvRepair':True}); Path('publisher-closed-world-contract.json').write_text(json.dumps(q,sort_keys=True,indent=2)+'\n')
          if os.environ['MODE']=='publisher' and Path(os.environ['PUBLISHER_PATH']).read_text()!=out: raise SystemExit('publisher byte mismatch')
          PY

      - name: Replay exact V37 global scan with independently reconstructed per-step historical environment
        shell: bash
        env:
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          GH_TOKEN: ${{ github.token }}
          GITHUB_TOKEN: ${{ github.token }}
          MODE: ${{ steps.mode.outputs.mode }}
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
        run: |
          set -euo pipefail
          git show "$PR890_HEAD:$V37_PATH" > v37.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v37.yml','v37-global-env.sh')
          PY
          source v37-global-env.sh
          git show "$PR889_HEAD:$V36_PATH" > v36.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v36.yml','v36-global-env.sh')
          PY
          source v36-global-env.sh
          git show "$PR888_HEAD:$V35_PATH" > v35.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v35.yml','v35-global-env.sh')
          PY
          source v35-global-env.sh
          git show "$PR886_HEAD:$V32_PATH" > v32.yml
          python - <<'PY'
          from pathlib import Path
          import shlex
          def env_from(src,dst):
              lines=Path(src).read_text().splitlines(); a=lines.index('    env:')+1; e=lines.index('    steps:'); out=[]
              for line in lines[a:e]:
                  if not line.startswith('      ') or ':' not in line: continue
                  k,v=line.strip().split(':',1); v=v.strip()
                  if len(v)>=2 and v[0]==v[-1] and v[0] in "'\"": v=v[1:-1]
                  if k in {'INFRA_BRANCH','INFRA_PATH','LIVE_CUTOFF'}: continue
                  out.append('export '+k+'='+shlex.quote(v))
              Path(dst).write_text('\n'.join(out)+'\n')
          env_from('v32.yml','v32-global-env.sh')
          PY
          source v32-global-env.sh
          export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v38"
          export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml"
          export LIVE_CUTOFF='5510140594'
          export CURRENT_LIVE_CUTOFF="$LIVE_CUTOFF"
          test -n "$PR888_HEAD"; test -n "$ALLOCATION_MARKER"; test "$LIVE_CUTOFF" = "$CURRENT_LIVE_CUTOFF"
          python - <<'PY'
          from pathlib import Path
          raw=Path('v37.yml').read_text(); name='Replay exact V36 global scan with preserved V36 replay-environment repairs'; a=raw.index(f'      - name: {name}\n'); s=raw.index('        run: |\n',a)+len('        run: |\n'); e=min(x for x in (raw.find('\n      - name:',s),raw.find('\n      - uses:',s)) if x!=-1); L=raw[s:e].splitlines(); assert all((not x) or x.startswith('          ') for x in L); x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n'
          repl=(
            ('export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v37"','export INFRA_BRANCH="review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v38"',1),
            ('export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v37.yml"','export INFRA_PATH=".github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml"',1),
            ("'5509304642'","'5510140594'",2),
          )
          for old,new,n in repl:
              if x.count(old)!=n: raise SystemExit('V37 global patch cardinality drift '+old+' '+str(x.count(old)))
              x=x.replace(old,new)
          Path('v37-global-replay.sh').write_text(x)
          PY
          bash v37-global-replay.sh
          export LIVE_CUTOFF="$CURRENT_LIVE_CUTOFF"
          test "$LIVE_CUTOFF" = '5510140594'
          python - <<'PY'
          import hashlib,json,os,subprocess
          from pathlib import Path
          scan=Path('repository-global-seed-scan.json')
          if not scan.is_file(): raise SystemExit('fresh repository-global scan missing after V38 replay')
          fresh=hashlib.sha256(scan.read_bytes()).hexdigest()
          q=json.load(open('v37-replay-environment-proof.json')); q.update({'successor':'v38','currentLiveCutoff':int(os.environ['LIVE_CUTOFF']),'v37PerStepHistoricalEnvRepair':True,'freshRepositoryGlobalScanLifecyclePhase':'post-inherited-global-scan','freshRepositoryGlobalScanSha256':fresh}); Path('v38-replay-environment-proof.json').write_text(json.dumps(q,sort_keys=True,indent=2)+'\n')
          r=json.load(open('review-receipt.json')); ids=list(r.get('predecessorDefects') or []); did=int(os.environ['V37_DEFECT']); ids.append(did) if did not in ids else None
          r.update({'schemaVersion':34,'status':'PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V38_ZERO_RUNTIME','headSha':os.environ['EVENT_HEAD'],'baseSha':os.environ['EVENT_BASE'],'reviewerBlob':subprocess.check_output(['git','hash-object',os.environ['INFRA_PATH']],text=True).strip(),'predecessorDefects':ids,'newerBlockerBaseline':int(os.environ['LIVE_CUTOFF']),'classifierSourceSha256':Path('v38-classifier.sha256').read_text().strip(),'canonicalPublisherSha256':Path('canonical-final-publisher.sha256').read_text().strip(),'v37FailedAttemptBound':True,'v37PerStepHistoricalEnvRepair':True,'v36FailedAttemptBound':True,'v36NestedV32StaticBindEnvRepair':True,'v35StaleGlobalScanResidueRepair':True,'v35HistoricalEnvLiveCutoffRebindRepair':True,'dispatchCreated':False,'consumedMarkerCreated':False,'workflowDispatchPerformed':False,'solverExecutionPerformed':False,'resultOpeningAuthorized':False,'levelBAdmissionAuthorized':False,'holdoutOpened':False,'productionAuthorized':False,'taylorOrJerusalemUsed':False,'newMappingAuthorized':False}); Path('review-receipt.json').write_text(json.dumps(r,sort_keys=True,indent=2)+'\n')
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38
          path: |
            review-receipt.json
            canonical-final-publisher.yml
            canonical-final-publisher.sha256
            publisher-closed-world-contract.json
            closed-world-hostile-fixtures.json
            v36-classifier-core.py
            v37-classifier-core.py
            v38-classifier-core.py
            v38-classifier.sha256
            v24-classifier-core.py
            v24-classifier.sha256
            v35-replay-environment-proof.json
            v36-replay-environment-proof.json
            v37-replay-environment-proof.json
            v38-replay-environment-proof.json
            v24-fixtures.json
            tracked-seed-scan.json
            repository-global-seed-scan.json
            repository-global-scan-success-attempt.txt
            final-global-scan-attempts/
          if-no-files-found: error
          compression-level: 0