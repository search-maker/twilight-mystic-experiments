name: Review AVPS Recovery4 ordinal45 final dispatch choreography v42

on:
  pull_request:
    types: [opened]
    branches: [main]
    paths:
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v42.yml
      - .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml

permissions: {contents: read, actions: read, issues: read, pull-requests: read}
concurrency:
  group: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v42-${{ github.event.pull_request.head.sha }}
  cancel-in-progress: false

jobs:
  review:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    env:
      INFRA_BASE: de3fafa052ff1d5b2f33ec0d4347c884d560d8b6
      INFRA_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v42
      INFRA_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v42.yml
      PUBLISHER_BRANCH: review/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1
      PUBLISHER_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-publisher-v1.yml
      PR894_HEAD: 0ba566eeb322048a3cb5e124a1d6e5539321ef7f
      PR894_TREE: b70bd4331e59a3f1167831c874641dab1106d7ff
      PR894_BLOB: 8379435bf8e05281e1a48b72bccc7e4917a9da97
      PR894_RUN: '33655027254'
      PR894_JOB: '100331332587'
      PR894_GENERIC: '33655027151'
      PR894_GENERIC_JOB: '100331332057'
      PR894_ARTIFACT: '9856982741'
      PR894_ARTIFACT_NAME: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v41
      PR894_ARTIFACT_DIGEST: sha256:e76accb67ff441e68ac07b16c2bb7ebd714ce6c9772cd4ad013c4747b5d5a222
      V41_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v41.yml
      V41_BACKUP_DEFECT: '5513122138'
      V41_PRIMARY_DEFECT: '5513125067'
      PR893_HEAD: 9868499cdd089c386e2e40ee4868e6f55df58433
      PR893_TREE: 71ee44e9f49217d2471be52708f9450352eebb3f
      PR893_BLOB: c29ef5e5244a7eb812a17202a5e18298a16e32fe
      PR893_RUN: '33648517440'
      PR893_JOB: '100309228018'
      PR893_GENERIC: '33648517457'
      PR893_GENERIC_JOB: '100309228189'
      V40_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v40.yml
      V40_DEFECT_STEP: '5512794498'
      V40_DEFECT: '5512796556'
      PR892_HEAD: 968e03ae5d1973ae611b4fa14b0b7c1f92736c9e
      PR892_TREE: 7fb5e58b16c42887cb2e444d1bb7d85548825250
      PR892_BLOB: a7411d0058dc61e9966e1ae5d9f6a88e62bb68cf
      PR892_RUN: '33641648385'
      PR892_JOB: '100285942753'
      PR892_GENERIC: '33641648191'
      PR892_GENERIC_JOB: '100285940630'
      V39_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v39.yml
      V39_DEFECT: '5511888009'
      PR891_HEAD: 082c912aa5babdfe3728c554eeb6cc53598173f6
      PR891_TREE: a6148b354d97396aa9fe18c1143aa6f19929daa7
      PR891_BLOB: 14e50de5d3e111a41e4ea06211d7e195fce02923
      PR891_RUN: '33635370018'
      PR891_JOB: '100264801376'
      PR891_GENERIC: '33635369848'
      PR891_GENERIC_JOB: '100264799989'
      V38_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v38.yml
      V38_DEFECT: '5510937307'
      V38_PRIMARY_DEFECT: '5511003079'
      V38_BACKUP_DEFECT: '5511070934'
      PR890_HEAD: 21d575825036b29840c7e021047f68e2fe6a0729
      PR890_TREE: 906ed4d00bf25582407381998f397d94b6d2e9e4
      PR890_BLOB: 315a20c8a4e8dd9414b954872d6d51eb97a4829f
      PR890_RUN: '33629118640'
      PR890_JOB: '100243891236'
      PR890_GENERIC: '33629118546'
      PR890_GENERIC_JOB: '100243890876'
      V37_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v37.yml
      V37_DEFECT: '5510140594'
      V36_DEFECT: '5509304642'
      V35_DEFECT: '5508214692'
      V35_RISK: '5508235083'
      PR888_HEAD: b7fa9a0af9b2f889e2db2544963ce98a77a00ca2
      PR888_BLOB: 590b1daea145d6519ed5b1b494a4e8364a099ff8
      V35_PATH: .github/workflows/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v35.yml
      LIVE_CUTOFF: '5513125067'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with: {ref: '${{ github.event.pull_request.head.sha }}', fetch-depth: 0, persist-credentials: false}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with: {python-version: '3.13'}

      - name: Replay exact V41 bind and bind rejected V41 receipt-ledger evidence
        id: mode
        shell: bash
        env:
          EVENT_BASE: ${{ github.event.pull_request.base.sha }}
          EVENT_BRANCH: ${{ github.event.pull_request.head.ref }}
          EVENT_HEAD: ${{ github.event.pull_request.head.sha }}
          EVENT_ACTION: ${{ github.event.action }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$EVENT_ACTION" = opened
          git fetch --no-tags origin "$PR894_HEAD"
          test "$(git rev-parse "$PR894_HEAD^{tree}")" = "$PR894_TREE"
          test "$(git rev-parse "$PR894_HEAD:$V41_PATH")" = "$PR894_BLOB"
          git show "$PR894_HEAD:$V41_PATH" > v41.yml
          python - <<'PY'
          import os
          from pathlib import Path
          OPEN='$'+'{{'; CLOSE='}}'; ACTION=OPEN+' github.event.action '+CLOSE
          def norm(s):
              if ACTION in s: s=s.replace(ACTION,os.environ['EVENT_ACTION'])
              if OPEN in s or CLOSE in s: raise SystemExit('residual/unrecognized Actions expression before bash')
              return s
          raw=Path('v41.yml').read_text(); name='Replay exact V38 bind with Actions-expression normalization and bind rejected V40 evidence'; a=raw.index(f'      - name: {name}\n'); s=raw.index('        run: |\n',a)+len('        run: |\n'); e=min(x for x in (raw.find('\n      - name:',s),raw.find('\n      - uses:',s)) if x!=-1); L=raw[s:e].splitlines(); assert all((not x) or x.startswith('          ') for x in L); x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n'
          b41='review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v41'; b42=os.environ['INFRA_BRANCH']; p41=os.environ['V41_PATH']; p42=os.environ['INFRA_PATH']; old="'5512796556'"; new="'5513125067'"
          if x.count(b41)!=1 or x.count(p41)!=1 or x.count(old)!=1: raise SystemExit('V41 bind successor patch cardinality drift')
          x=x.replace(b41,b42).replace(p41,p42).replace(old,new)
          x=norm(x); Path('v41-bind-replay.sh').write_text(x)
          PY
          bash v41-bind-replay.sh
          gh api "repos/$GITHUB_REPOSITORY/pulls/894" > p41.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR894_RUN" > r41.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR894_RUN/jobs?per_page=100" > j41s.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR894_GENERIC" > g41.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR894_GENERIC/jobs?per_page=100" > gj41s.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$PR894_RUN/artifacts?per_page=100" > a41.json
          gh api --paginate --slurp "repos/$GITHUB_REPOSITORY/issues/60/comments?per_page=100" > c42.json
          python - <<'PY'
          import json,os
          z=lambda p:json.load(open(p)); p,r,g=z('p41.json'),z('r41.json'),z('g41.json'); js=z('j41s.json').get('jobs',[]); gjs=z('gj41s.json').get('jobs',[])
          if p.get('state')!='closed' or p.get('merged_at') is not None or p['head']['sha']!=os.environ['PR894_HEAD'] or p['base']['sha']!=os.environ['INFRA_BASE']: raise SystemExit('PR894 drift')
          if r['id']!=int(os.environ['PR894_RUN']) or r['head_sha']!=os.environ['PR894_HEAD'] or r['run_attempt']!=1 or r['conclusion']!='success': raise SystemExit('V41 dedicated drift')
          h=[x for x in js if int(x.get('id') or 0)==int(os.environ['PR894_JOB'])]
          if len(h)!=1 or h[0].get('conclusion')!='success': raise SystemExit('V41 job drift')
          if g['id']!=int(os.environ['PR894_GENERIC']) or g['head_sha']!=os.environ['PR894_HEAD'] or g['run_attempt']!=1 or g['conclusion']!='success': raise SystemExit('V41 generic drift')
          gh=[x for x in gjs if int(x.get('id') or 0)==int(os.environ['PR894_GENERIC_JOB'])]
          if len(gh)!=1 or gh[0].get('conclusion')!='success': raise SystemExit('V41 generic job drift')
          A=z('a41.json').get('artifacts',[])
          if len(A)!=1: raise SystemExit('V41 artifact cardinality drift')
          a=A[0]
          if int(a.get('id') or 0)!=int(os.environ['PR894_ARTIFACT']) or a.get('name')!=os.environ['PR894_ARTIFACT_NAME'] or a.get('digest')!=os.environ['PR894_ARTIFACT_DIGEST'] or a.get('expired') is not False: raise SystemExit('V41 artifact identity/digest drift')
          C=[x for q in z('c42.json') for x in q]
          expected={
            int(os.environ['V41_BACKUP_DEFECT']):'BACKUP_INDEPENDENT_AUDIT::CONFIRMED_DEFECT V41_PR894_ZERO_RUNTIME_RECEIPT_SUPPRESSION_LEDGER_STALE',
            int(os.environ['V41_PRIMARY_DEFECT']):'INDEPENDENT_AUDIT::CONFIRMED_DEFECT AVPS_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_INFRA_V41_PR894_REVIEW_RECEIPT_EXACT_SUPPRESSION_LEDGER_MISMATCH'}
          for cid,pref in expected.items():
              h=[x for x in C if int(x.get('id') or 0)==cid]
              if len(h)!=1 or not str(h[0].get('body') or '').startswith(pref): raise SystemExit('V41 decision binding '+str(cid))
              u=h[0].get('user') or {}; b=str(h[0].get('body') or '')
              if u.get('login')!='search-maker' or int(u.get('id') or 0)!=304153003 or h[0].get('author_association')!='OWNER' or 'QUARANTINE_DOWNSTREAM=false' not in b or 'NON_AUTHORIZING=true' not in b: raise SystemExit('V41 decision identity '+str(cid))
          PY

      - uses: actions/download-artifact@v4
        with: {name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v25, path: v25, repository: '${{ github.repository }}', run-id: 33579241722, github-token: '${{ github.token }}'}

      - name: Replay exact V41 build and bind V42 final suppression ledger
        shell: bash
        env: {MODE: '${{ steps.mode.outputs.mode }}', EVENT_ACTION: '${{ github.event.action }}'}
        run: |
          set -euo pipefail
          git show "$PR894_HEAD:$V41_PATH" > v41.yml
          python - <<'PY'
          import os
          from pathlib import Path
          OPEN='$'+'{{'; CLOSE='}}'; ACTION=OPEN+' github.event.action '+CLOSE
          def norm(s):
              if ACTION in s: s=s.replace(ACTION,os.environ['EVENT_ACTION'])
              if OPEN in s or CLOSE in s: raise SystemExit('residual/unrecognized Actions expression before bash')
              return s
          raw=Path('v41.yml').read_text(); name='Replay exact V38 build with Actions-expression guard and bind V41 predecessor ledger'; a=raw.index(f'      - name: {name}\n'); s=raw.index('        run: |\n',a)+len('        run: |\n'); e=min(x for x in (raw.find('\n      - name:',s),raw.find('\n      - uses:',s)) if x!=-1); L=raw[s:e].splitlines(); assert all((not x) or x.startswith('          ') for x in L); x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n'
          b41='review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v41'; b42=os.environ['INFRA_BRANCH']; p41=os.environ['V41_PATH']; p42=os.environ['INFRA_PATH']; old="'5512796556'"; new="'5513125067'"
          if x.count(b41)!=1 or x.count(p41)!=1 or x.count(old)!=1: raise SystemExit('V41 build successor patch cardinality drift')
          x=x.replace(b41,b42).replace(p41,p42).replace(old,new)
          tail="PY\nbash v38-build-replay.sh\n"
          if x.count(tail)!=1: raise SystemExit('V41 build execution tail drift')
          deferred='\n'.join([
              'PY',
              "python - <<'PYV42'",
              'from pathlib import Path',
              "p=Path('v38-build-replay.sh'); y=p.read_text()",
              "marker=\"if os.environ['MODE']=='publisher' and Path(os.environ['PUBLISHER_PATH']).read_text()!=out: raise SystemExit('publisher byte mismatch')\"",
              "if y.count(marker)!=1: raise SystemExit('V41 publisher equality marker drift')",
              "y=y.replace(marker,\"if os.environ['MODE']=='publisher': pass\",1); p.write_text(y)",
              'PYV42',
              'bash v38-build-replay.sh'])+'\n'
          x=x.replace(tail,deferred,1)
          x=norm(x); Path('v41-build-replay.sh').write_text(x)
          PY
          bash v41-build-replay.sh
          python - <<'PY'
          import hashlib,importlib.util,json,os,re
          from pathlib import Path
          old=Path('v41-classifier-core.py').read_text().rstrip('\n')
          m=re.findall(r'^B=\{([0-9,]+)\}$',old,re.M)
          if len(m)!=1: raise SystemExit('V41 classifier ledger shape drift')
          vals=[int(z) for z in m[0].split(',') if z]; add=[int(os.environ['V41_BACKUP_DEFECT']),int(os.environ['V41_PRIMARY_DEFECT'])]
          if len(vals)!=35 or len(set(vals))!=35 or int(os.environ['V40_DEFECT']) not in vals or any(d in vals for d in add): raise SystemExit('V41 classifier ledger content drift')
          vals2=vals+add
          new=old.replace('B={'+m[0]+'}','B={'+','.join(map(str,vals2))+'}',1)
          out=Path('canonical-final-publisher.yml').read_text(); oh='\n'.join(('          '+z if z else '') for z in old.splitlines())+'\n'; nh='\n'.join(('          '+z if z else '') for z in new.splitlines())+'\n'
          if out.count(oh)!=1: raise SystemExit('V42 embedded classifier patch drift')
          out=out.replace(oh,nh,1); Path('canonical-final-publisher.yml').write_text(out)
          Path('v42-classifier-core.py').write_text(new+'\n'); cd=hashlib.sha256(new.encode()).hexdigest(); pd=hashlib.sha256(out.encode()).hexdigest(); Path('v42-classifier.sha256').write_text(cd+'\n'); Path('canonical-final-publisher.sha256').write_text(pd+'\n')
          sp=importlib.util.spec_from_file_location('c42','v42-classifier-core.py'); mod=importlib.util.module_from_spec(sp); sp.loader.exec_module(mod)
          if sorted(mod.B)!=sorted(vals2) or len(mod.B)!=37: raise SystemExit('V42 executable ledger mismatch')
          if any(mod.avps_governance_blocker('CONFIRMED_DEFECT AVPS_X',d) for d in add) or not mod.avps_governance_blocker('confirmed_defect avps_x',int(os.environ['LIVE_CUTOFF'])+1): raise SystemExit('V42 helper semantics')
          q=json.load(open('publisher-closed-world-contract.json')); q.update({'liveGovernanceClassifier':'structured-firstline-closure-v42','classifierSourceSha256':cd,'exactSuppressedPredecessorIds':sorted(mod.B),'newerBlockerBaseline':int(os.environ['LIVE_CUTOFF']),'canonicalPublisherSha256':pd,'v41ReceiptSuppressionLedgerRepair':True}); Path('publisher-closed-world-contract.json').write_text(json.dumps(q,sort_keys=True,indent=2)+'\n')
          if os.environ['MODE']=='publisher' and Path(os.environ['PUBLISHER_PATH']).read_text()!=out: raise SystemExit('publisher byte mismatch')
          PY

      - name: Replay exact V41 global scan and rewrite receipt from final classifier ledger
        shell: bash
        env: {EVENT_BRANCH: '${{ github.event.pull_request.head.ref }}', EVENT_HEAD: '${{ github.event.pull_request.head.sha }}', EVENT_ACTION: '${{ github.event.action }}', GH_TOKEN: '${{ github.token }}', GITHUB_TOKEN: '${{ github.token }}', MODE: '${{ steps.mode.outputs.mode }}', EVENT_BASE: '${{ github.event.pull_request.base.sha }}'}
        run: |
          set -euo pipefail
          git show "$PR894_HEAD:$V41_PATH" > v41.yml
          python - <<'PY'
          import os
          from pathlib import Path
          OPEN='$'+'{{'; CLOSE='}}'; ACTION=OPEN+' github.event.action '+CLOSE
          def norm(s):
              if ACTION in s: s=s.replace(ACTION,os.environ['EVENT_ACTION'])
              if OPEN in s or CLOSE in s: raise SystemExit('residual/unrecognized Actions expression before bash')
              return s
          raw=Path('v41.yml').read_text(); name='Replay exact V38 global scan with V35 self-path materialization, Actions-expression guard, and outer evidence bind'; a=raw.index(f'      - name: {name}\n'); s=raw.index('        run: |\n',a)+len('        run: |\n'); e=min(x for x in (raw.find('\n      - name:',s),raw.find('\n      - uses:',s)) if x!=-1); L=raw[s:e].splitlines(); assert all((not x) or x.startswith('          ') for x in L); x='\n'.join(y[10:] if y.startswith('          ') else '' for y in L)+'\n'
          b41='review/avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-infrastructure-v41'; b42=os.environ['INFRA_BRANCH']; p41=os.environ['V41_PATH']; p42=os.environ['INFRA_PATH']; old="'5512796556'"; new="'5513125067'"
          if x.count(b41)!=1 or x.count(p41)!=1 or x.count(old)!=2: raise SystemExit('V41 global successor patch cardinality drift')
          x=x.replace(b41,b42).replace(p41,p42).replace(old,new)
          x=norm(x); Path('v41-global-replay.sh').write_text(x)
          PY
          bash v41-global-replay.sh
          export LIVE_CUTOFF='5513125067'; export CURRENT_LIVE_CUTOFF="$LIVE_CUTOFF"
          python - <<'PY'
          import hashlib,importlib.util,json,os,subprocess
          from pathlib import Path
          scan=Path('repository-global-seed-scan.json')
          if not scan.is_file(): raise SystemExit('fresh repository-global scan missing after V42 replay')
          sp=importlib.util.spec_from_file_location('c42','v42-classifier-core.py'); mod=importlib.util.module_from_spec(sp); sp.loader.exec_module(mod); final=sorted(mod.B)
          if len(final)!=37 or len(set(final))!=37: raise SystemExit('V42 final classifier cardinality drift')
          q=json.load(open('publisher-closed-world-contract.json'))
          if q.get('exactSuppressedPredecessorIds')!=final: raise SystemExit('V42 publisher-contract ledger mismatch before receipt rewrite')
          r=json.load(open('review-receipt.json')); stale=list(r.get('exactSuppressedPredecessorIds') or [])
          if len(stale)!=11 or stale[-1]!=5503366764 or stale==final: raise SystemExit('V41 stale receipt fixture drift')
          fixture={'source':'V41 uploaded receipt replay','staleExactSuppressedPredecessorIds':stale,'finalClassifierExactSuppressedPredecessorIds':final,'staleCount':len(stale),'finalCount':len(final),'rewrittenToFinal':True}
          ids=list(r.get('predecessorDefects') or []); add=[int(os.environ['V41_BACKUP_DEFECT']),int(os.environ['V41_PRIMARY_DEFECT'])]; [ids.append(d) for d in add if d not in ids]
          r.update({'schemaVersion':38,'status':'PASS_AVPS_V2_RECOVERY4_ORDINAL45_FINAL_DISPATCH_CHOREOGRAPHY_REVIEW_V42_ZERO_RUNTIME','headSha':os.environ['EVENT_HEAD'],'baseSha':os.environ['EVENT_BASE'],'reviewerBlob':subprocess.check_output(['git','hash-object',os.environ['INFRA_PATH']],text=True).strip(),'predecessorDefects':ids,'newerBlockerBaseline':int(os.environ['LIVE_CUTOFF']),'classifierSourceSha256':Path('v42-classifier.sha256').read_text().strip(),'canonicalPublisherSha256':Path('canonical-final-publisher.sha256').read_text().strip(),'exactSuppressedPredecessorIds':final,'v41ReceiptSuppressionLedgerRepair':True,'v41RejectedReviewerHead':os.environ['PR894_HEAD'],'v41RejectedReviewerBlob':os.environ['PR894_BLOB'],'v41ArtifactId':int(os.environ['PR894_ARTIFACT']),'v41ArtifactDigest':os.environ['PR894_ARTIFACT_DIGEST'],'v41BackupDefect':int(os.environ['V41_BACKUP_DEFECT']),'v41PrimaryDefect':int(os.environ['V41_PRIMARY_DEFECT']),'dispatchCreated':False,'consumedMarkerCreated':False,'workflowDispatchPerformed':False,'solverExecutionPerformed':False,'resultOpeningAuthorized':False,'levelBAdmissionAuthorized':False,'holdoutOpened':False,'productionAuthorized':False,'taylorOrJerusalemUsed':False,'newMappingAuthorized':False})
          Path('review-receipt.json').write_text(json.dumps(r,sort_keys=True,indent=2)+'\n'); Path('v42-receipt-ledger-fixture.json').write_text(json.dumps(fixture,sort_keys=True,indent=2)+'\n')
          p=json.load(open('v41-replay-environment-proof.json')); p.update({'successor':'v42','currentLiveCutoff':int(os.environ['LIVE_CUTOFF']),'v41ReceiptSuppressionLedgerRepair':True,'freshRepositoryGlobalScanSha256':hashlib.sha256(scan.read_bytes()).hexdigest()}); Path('v42-replay-environment-proof.json').write_text(json.dumps(p,sort_keys=True,indent=2)+'\n')
          rr=json.load(open('review-receipt.json')); qq=json.load(open('publisher-closed-world-contract.json'))
          if rr.get('exactSuppressedPredecessorIds')!=final or qq.get('exactSuppressedPredecessorIds')!=final or sorted(mod.B)!=final: raise SystemExit('V42 three-way exact suppression ledger mismatch')
          if rr.get('exactSuppressedPredecessorIds')!=sorted(rr.get('exactSuppressedPredecessorIds')): raise SystemExit('V42 receipt suppression ledger not sorted')
          if rr.get('canonicalPublisherSha256')!=hashlib.sha256(Path('canonical-final-publisher.yml').read_bytes()).hexdigest(): raise SystemExit('V42 receipt publisher hash mismatch')
          PY

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: avps-v2-recovery4-ordinal45-final-dispatch-choreography-review-v42
          path: |
            review-receipt.json
            canonical-final-publisher.yml
            canonical-final-publisher.sha256
            publisher-closed-world-contract.json
            closed-world-hostile-fixtures.json
            v36-classifier-core.py
            v37-classifier-core.py
            v38-classifier-core.py
            v41-classifier-core.py
            v41-classifier.sha256
            v42-classifier-core.py
            v42-classifier.sha256
            v24-classifier-core.py
            v24-classifier.sha256
            v35-replay-environment-proof.json
            v36-replay-environment-proof.json
            v37-replay-environment-proof.json
            v38-replay-environment-proof.json
            v41-replay-environment-proof.json
            v42-replay-environment-proof.json
            v42-receipt-ledger-fixture.json
            v24-fixtures.json
            tracked-seed-scan.json
            repository-global-seed-scan.json
            repository-global-scan-success-attempt.txt
            final-global-scan-attempts/
          if-no-files-found: error
          compression-level: 0
