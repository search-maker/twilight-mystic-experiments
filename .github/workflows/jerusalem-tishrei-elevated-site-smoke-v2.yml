name: Jerusalem Tishrei elevated-site infrastructure smoke v2
run-name: Tishrei elevation smoke v2 | key=${{ inputs.execution_key }} | auth=${{ inputs.authorization_ref }}

on:
  workflow_dispatch:
    inputs:
      authorization_ref:
        description: Exact one-purpose infrastructure-smoke authorization commit SHA
        required: true
        type: string
      execution_key:
        description: Exact infrastructure-smoke execution key
        required: true
        type: string
      smoke_authorization_ordinal:
        description: Exact infrastructure-smoke authorization ordinal
        required: true
        type: string

permissions:
  contents: read
  actions: read

concurrency:
  group: jerusalem-tishrei-elevated-site-smoke-v2-${{ inputs.execution_key }}
  cancel-in-progress: false

env:
  PACKAGE: experiments/jerusalem-tishrei-elevated-site-smoke-v2
  AUTHORIZATION: experiments/jerusalem-tishrei-elevated-site-smoke-v2/authorization.smoke.json
  SMOKE_MANIFEST: experiments/jerusalem-tishrei-elevated-site-smoke-v2/manifest.smoke.json
  ADAPTER: experiments/jerusalem-tishrei-elevated-site-smoke-v2/adapter.py
  SMOKE_EXECUTOR: experiments/jerusalem-tishrei-elevated-site-smoke-v2/smoke_executor.py
  GUARD: experiments/jerusalem-tishrei-elevated-site-smoke-v2/execution_guard.py
  RUNTIME_LOCK: experiments/mystic-batch-v1/runtime-lock.micromamba.json
  RUNTIME_PROBE: experiments/mystic-batch-v1/runtime_probe.py

jobs:
  preflight:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
      timeout_seconds: ${{ steps.matrix.outputs.timeout_seconds }}
    steps:
      - name: Check out exact one-purpose smoke authorization commit with full ancestry
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.authorization_ref }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.4'
      - name: Verify exact infrastructure-only authorization
        run: >-
          python "$GUARD"
          --repository-root .
          --authorization-ref "${{ inputs.authorization_ref }}"
          --execution-key "${{ inputs.execution_key }}"
          --smoke-authorization-ordinal "${{ inputs.smoke_authorization_ordinal }}"
          --output preflight/authorization-guard.json
      - name: Refuse duplicate smoke key/ref/ordinal before uvspec
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          python experiments/mystic-batch-v1/duplicate_run_audit.py
          --repository "${{ github.repository }}"
          --workflow jerusalem-tishrei-elevated-site-smoke-v2.yml
          --current-run-id "${{ github.run_id }}"
          --execution-key "${{ inputs.execution_key }}"
          --authorization-ref "${{ inputs.authorization_ref }}"
          --authorization-ordinal "${{ inputs.smoke_authorization_ordinal }}"
          --token "$GH_TOKEN"
          --output preflight/duplicate-run-audit.json
      - name: Freeze exact two-case smoke matrix
        id: matrix
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json, os
          from pathlib import Path
          m=json.loads(Path(os.environ['SMOKE_MANIFEST']).read_text())
          assert m['stageId']=='jerusalem-tishrei-elevated-site-smoke-v2'
          assert m['infrastructureOnly'] is True and m['scientificUseProhibited'] is True
          assert len(m['cases'])==2 and sum(c['photonHistories'] for c in m['cases'])==20000
          assert {c['method'] for c in m['cases']}=={'reference-vroom','alis'}
          matrix={'include':[{'case_id':c['caseId'],'method':c['method'],'seed':c['seed'],'photon_histories':c['photonHistories']} for c in m['cases']]}
          out=Path(os.environ['GITHUB_OUTPUT'])
          with out.open('a') as f:
              f.write('matrix='+json.dumps(matrix,separators=(',',':'))+'\n')
              f.write('timeout_seconds='+str(m['limits']['perCaseTimeoutSeconds'])+'\n')
          Path('preflight/matrix.json').write_text(json.dumps(matrix,indent=2,sort_keys=True)+'\n')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jerusalem-tishrei-elevated-site-smoke-v2-preflight
          path: preflight
          if-no-files-found: warn
          retention-days: 90

  cases:
    needs: preflight
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix: ${{ fromJSON(needs.preflight.outputs.matrix) }}
    steps:
      - name: Check out exact one-purpose smoke authorization commit
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.authorization_ref }}
          fetch-depth: 2
          persist-credentials: false
      - uses: mamba-org/setup-micromamba@v2
        with:
          environment-name: jerusalem-tishrei-elevated-site-smoke-v2
          create-args: >-
            -c conda-forge
            python=3.12.4
            rubin-libradtran=2.0.6=py312pl5321he9373c2_1
          cache-environment: true
          cache-downloads: true
      - name: Recompute exact runtime identity
        shell: bash -el {0}
        run: |
          set -euo pipefail
          UVSPEC="$(command -v uvspec)"
          DATA_DIR="$CONDA_PREFIX/share/libRadtran/data"
          ATMOSPHERE="$DATA_DIR/atmmod/afglus.dat"
          test -x "$UVSPEC"
          test -s "$ATMOSPHERE"
          python "$RUNTIME_PROBE" \
            --uvspec "$UVSPEC" \
            --data-dir "$DATA_DIR" \
            --atmosphere "$ATMOSPHERE" \
            --runtime-lock "$RUNTIME_LOCK" \
            --output "runtime-reports/${{ matrix.case_id }}/runtime-report.json"
      - name: Execute exactly one infrastructure-smoke case
        shell: bash -el {0}
        run: |
          set -euo pipefail
          UVSPEC="$(command -v uvspec)"
          DATA_DIR="$CONDA_PREFIX/share/libRadtran/data"
          python "$SMOKE_EXECUTOR" \
            --smoke-manifest "$SMOKE_MANIFEST" \
            --runtime-report "runtime-reports/${{ matrix.case_id }}/runtime-report.json" \
            --adapter "$ADAPTER" \
            --case-id "${{ matrix.case_id }}" \
            --data-dir "$DATA_DIR" \
            --repository-root "$GITHUB_WORKSPACE" \
            --uvspec "$UVSPEC" \
            --output-root smoke-output \
            --timeout-seconds "${{ needs.preflight.outputs.timeout_seconds }}" \
            --allow-infrastructure-smoke
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jerusalem-tishrei-elevated-site-smoke-v2-case-${{ matrix.case_id }}
          path: |
            smoke-output
            runtime-reports
          if-no-files-found: warn
          retention-days: 90

  audit:
    needs: [preflight, cases]
    if: always() && needs.preflight.result == 'success'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Check out exact one-purpose smoke authorization commit
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.authorization_ref }}
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12.4'
      - uses: actions/download-artifact@v4
        with:
          pattern: jerusalem-tishrei-elevated-site-smoke-v2-case-*
          path: smoke-case-artifacts
          merge-multiple: true
      - name: Audit exact infrastructure-only smoke universe
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib, json
          from pathlib import Path
          paths=sorted(Path('smoke-case-artifacts').rglob('smoke-case-result.json'))
          records=[json.loads(p.read_text()) for p in paths]
          failures=[]
          expected={'jtm-smoke-gamcyg-vroom-v2':'reference-vroom','jtm-smoke-gamcyg-alis-v2':'alis'}
          by_id={r.get('caseId'):r for r in records}
          if set(by_id)!=set(expected): failures.append({'code':'case-universe','actual':sorted(str(x) for x in by_id)})
          for case_id,method in expected.items():
              r=by_id.get(case_id,{})
              checks={
                  'status':'INFRASTRUCTURE_SMOKE_COMPLETED',
                  'infrastructureOnly':True,
                  'scientificDiagnostic':False,
                  'scientificUseProhibited':True,
                  'successDoesNotAuthorizeScientificExecution':True,
                  'successDoesNotAuthorizeProduction':True,
                  'method':method,
                  'photonHistories':10000,
                  'syntaxCheckCount':1,
                  'solverExecutionCount':1,
              }
              stale={k:(r.get(k),v) for k,v in checks.items() if r.get(k)!=v}
              if stale: failures.append({'code':'case-invariant','caseId':case_id,'stale':stale})
              if (r.get('syntax') or {}).get('exitCode')!=0 or (r.get('syntax') or {}).get('timedOut') is not False:
                  failures.append({'code':'syntax','caseId':case_id})
              if (r.get('solver') or {}).get('exitCode')!=0 or (r.get('solver') or {}).get('timedOut') is not False:
                  failures.append({'code':'solver','caseId':case_id})
              s=r.get('structuralOutput') or {}
              if method=='reference-vroom' and s.get('nodeCount')!=17: failures.append({'code':'vroom-grid','caseId':case_id,'detail':s})
              if method=='alis' and s.get('nodeCount')!=8001: failures.append({'code':'alis-grid','caseId':case_id,'detail':s})
              for key in ('radianceOutputSha256','stdOutputSha256','inputResolvedSha256'):
                  value=r.get(key)
                  if not isinstance(value,str) or len(value)!=64: failures.append({'code':'hash','caseId':case_id,'field':key})
          report={
              'schemaVersion':1,
              'stageId':'jerusalem-tishrei-elevated-site-smoke-v2',
              'status':'INFRASTRUCTURE_SMOKE_PASSED' if not failures else 'INFRASTRUCTURE_SMOKE_FAILED',
              'scientificUseProhibited':True,
              'caseCount':len(records),
              'configuredPhotonHistoriesSum':sum(int(r.get('photonHistories',0)) for r in records),
              'failures':failures,
              'caseResultHashes':{p.parent.name:hashlib.sha256(p.read_bytes()).hexdigest() for p in paths},
              'boundary':'structural infrastructure audit only; no scientific statistic or validity claim is permitted'
          }
          Path('smoke-audit').mkdir(exist_ok=True)
          Path('smoke-audit/smoke-audit.json').write_text(json.dumps(report,indent=2,sort_keys=True)+'\n')
          print(json.dumps(report,indent=2,sort_keys=True))
          if failures: raise SystemExit(2)
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: jerusalem-tishrei-elevated-site-smoke-v2-audit
          path: smoke-audit
          if-no-files-found: warn
          retention-days: 90
