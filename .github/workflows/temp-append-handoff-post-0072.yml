name: Temporary append cumulative handoff post-0072

on:
  push:
    branches:
      - 'docs/update-2026-08-16-handoff-post-0072'

permissions:
  contents: write

jobs:
  append:
    if: github.actor == 'search-maker'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: docs/update-2026-08-16-handoff-post-0072
          fetch-depth: 1
      - name: Append post-creation delta exactly once
        shell: bash
        run: |
          set -euo pipefail
          f='review/full-spectrum-estimator-pilot-v2/STAR_VISIBILITY_PROJECT_HANDOFF_2026-08-16.md'
          marker='## 34. POST-CREATION DELTA — MYSTIC-STATE-0072 COMPLETE / ORDINAL28 PASS / VALIDATED PACKAGE PUBLISHED'
          if grep -Fq "$marker" "$f"; then
            echo 'delta already present; refusing duplicate append' >&2
            exit 2
          fi
          cat >> "$f" <<'HANDOFF_DELTA_EOF'

---

# CURRENT OVERRIDE — POST-CREATION UPDATE, 2026-08-16 ~02:32 EDT

> **This section is newer than every dynamic status above. Read it first for live project state.** The original body of this cumulative 2026-08-16 handoff is intentionally preserved byte-for-byte. This appended delta records everything material that changed after that handoff file was created through the completion of `MYSTIC-STATE-0072`, the ordinal28 protected-validation PASS, and publication of the computationally validated surrogate package. Historical statements above remain useful as chronology but are superseded wherever they conflict with this section.

## 34. POST-CREATION DELTA — MYSTIC-STATE-0072 COMPLETE / ORDINAL28 PASS / VALIDATED PACKAGE PUBLISHED

### 34.1 Canonical science repository state at this update

Repository: `search-maker/twilight-mystic-experiments`

Canonical `main` after the validated-package publication merge:

`0773ef6acff167b17acdcd30f52a5190b3c2280b`

Issue #60 remains the sole authoritative governance/control ledger.

The active/latest governance state is now **`MYSTIC-STATE-0072`**, created after the review-only fresh-source preregistration under 0071. `MYSTIC-STATE-0072` has completed its authorized scientific work successfully; it does **not** itself authorize production or Worker-B/Worker-C activation.

### 34.2 What 0071 had already frozen before 0072

The completed `MYSTIC-STATE-0071` training-only reset remains immutable:

- exact frozen changed-model canonical SHA-256: `c4902eb3c2ba67b12dc4ef2b9cefb67c5963a6abc104708a73b2aab5dd0163b9`;
- selected candidate: `resid-V1_IDW_COS_COORDINATES-r1e-05-k6-p1-a1`;
- model kind: `RIDGE_PRIMARY_RESIDUAL_IDW_SHAPE_IDW`;
- frozen model-artifact canonical SHA-256: `d7f77416c782dd6226be0898f722fb880096638156517177cf1252b96b66f015`;
- exact materialization run: `31927414786`, attempt 1, SUCCESS;
- materialization artifact: `9258264586`, digest `sha256:a3a4266e95919dccc8248e73f16fc0960dae4254543cbf48677b1f780530bfc7`;
- exact model member byte SHA-256: `0b850664584244abdc781f87ce9e5b89cdab28b08a2999612b155378cbe42d79`;
- frozen training artifact: `9229229366`, digest `sha256:f4c8c68a622f7c6bdc1b9177ad31d22f673becb1f286436d54b876ceece3668a`;
- frozen spectral-representation package SHA-256: `2491ac91ed924f2ba69b37ea20f48d63f51d41146cd9fe50e0bd63bfb315a763`;
- no ordinal27 protected value was read during v3 fitting/materialization.

The original ordinal27 source remains permanently diagnostic-only. Its terminal status is still `FAIL_FROZEN_FRESH_DOD_NO_RETUNING`; its values may never be reused as fresh validation after retuning.

### 34.3 Completely fresh source prepared after the handoff was created

PR #222, `Preregister Level-B v3 future untouched validation source`, merged to main as:

`6df161627dacdf8592ae3226e8f366e0e507e195`

Canonical source contract:

`review/level-b-v3-future-fresh-validation-source-v1/contract-v1.json`

Git blob:

`105b1560dbb1dacaba891a6828ca04a28d756e6b`

The source was selected using only the immutable 58 training geometry inputs. Its design deliberately used normalized lattice levels `{0.2,0.4,0.6,0.8}` while the earlier public v0070 candidate lattice used `{0.1,0.3,0.5,0.7,0.9}`. Those level sets are disjoint in every coordinate, so every new candidate is structurally distinct from every prior v0070 lattice candidate without needing to read the prior selected holdout identities or coordinates.

From 1024 new lattice points, the unchanged training-support band `[0.30,0.60]` left 601 candidates. Deterministic training-only farthest-point selection froze six geometries:

1. `future-fresh-source-01`: Sun dep 3.7°, target alt 50°, rel az 53.13010235415597°, elev 2000 m, AOD550 0.12;
2. `future-fresh-source-02`: 3.7°, 35°, 126.86989764584402°, 2000 m, 0.33;
3. `future-fresh-source-03`: 8.8°, 20°, 53.13010235415597°, 1000 m, 0.33;
4. `future-fresh-source-04`: 7.1°, 65°, 126.86989764584402°, 500 m, 0.33;
5. `future-fresh-source-05`: 5.4°, 65°, 101.53695903281549°, 2000 m, 0.26;
6. `future-fresh-source-06`: 3.7°, 20°, 53.13010235415597°, 1000 m, 0.33.

The source merge opened no protected values, allocated no ordinal/seeds, and authorized no MYSTIC. A repository-wide exact collision audit was required again immediately before authorization; it passed.

### 34.4 `MYSTIC-STATE-0072` — frozen fresh-validation generation

Issue #60 directive comment:

`5305940266`

Activation main:

`6df161627dacdf8592ae3226e8f366e0e507e195`

0072 froze one independent protected-validation generation for the exact v3 model and exact six-source geometry set. It explicitly prohibited ordinal27 influence, retuning, threshold relaxation, support changes, and source point replacement after activation.

Frozen execution envelope:

- scientific ordinal: **28**;
- seeds: `2110000001..2110000024`, exactly four sequential seeds per geometry;
- authorization branch: `authorization/level-b-v3-fresh-validation-ordinal28-v1`;
- allocation-lock branch: `allocation/level-b-v3-fresh-validation-ordinal28-v1`;
- dispatch branch: `dispatch/level-b-v3-fresh-validation-ordinal28-v1`;
- execution key: `level-b-v3:fresh-protected-validation:28`;
- six geometries × four independent blocks = 24 cases;
- 40,000,000 photon histories per block;
- 960,000,000 configured histories total;
- ALIS spectral importance sampling at 550 nm;
- 380–780 nm on the same canonical 8001-node token grid;
- same reviewed libRadtran/MYSTIC runtime, atmosphere and physics identity used for ordinal27;
- exactly one syntax check + one MYSTIC invocation per case;
- no adaptive blocks, point replacement, GitHub rerun, retry or resume.

Frozen Definition of Done was preserved **without relaxation from ordinal27**:

- every geometry inside the reviewed physical design box and nearest normalized frozen-training distance `<=0.60`;
- separately for photopic/scotopic/Johnson-V: absolute mean signed log bias `<=0.08`, median absolute log error `<=0.15`, worst absolute log error `<=0.35`, worst uncertainty-normalized error `<=3.0` using unchanged direct-MYSTIC uncertainty semantics + fixed one-sigma surrogate log budget `0.12`;
- spectral shape median per-case NRMSE `<=0.75`, worst per-case NRMSE `<=1.25`, worst single PCA-coefficient normalized error `<=3.0`;
- unchanged 58-training transformed-primary mean predictor `[0.3993901995212697, 1.7062844994448103, -3.8475190646906268]` evaluated on the same six future truths; surrogate/baseline aggregate primary MALE ratio `<=0.70`;
- exact-zero semantics preserved; no epsilon substitution; all gates must pass; P90/P95 are not principal metrics at n=6.

### 34.5 Review/transport chain before ordinal28 execution

PR #223 — `Freeze Level-B v3 fresh validation implementation for ordinal28`:

- review-only, no protected value opening/MYSTIC/allocation;
- exact reviewed head `352404d0ac2b3f8ebccf88fd43026e879b26e84d`;
- merge commit `969748e765fafd68d8876f1c1bd311b0fd712031`;
- evaluator reused the frozen ordinal27 truth-projection/uncertainty/support/DoD math and changed only predictor compatibility for the new v3 hybrid model;
- review parity required exact v3 primary predictions on all 58 training geometries to `<=1e-12` and exactly zero shape change;
- repository-wide geometry collision and ordinal/seed freshness checks passed.

PR #224 — `Bind ordinal28 one-use protected-validation transport`:

- review-only authorization/allocation/dispatch transport;
- reviewed head `8ea600e4ee83350b94cf99ed878ad379c77300eb`;
- dedicated transport review `31930266090` SUCCESS;
- repo-wide contract `31930266080` SUCCESS;
- merge commit `6c1e352c566414f60ab788b3d73c66d6fbaa5ea1`;
- preserved atomic allocation-lock + logical idempotent Issue-marker semantics;
- exact artifact ZIP `tmp/` path semantics preserved;
- no science/protected values occurred in review.

PR #225 — `Authorize one-use Level-B v3 fresh validation ordinal28 attempt 1`:

- Draft, exactly one commit / one authorization JSON file;
- parent main `6c1e352c566414f60ab788b3d73c66d6fbaa5ea1`;
- authorization head `54d7e35c45b6641065fc6670464e255bb790e102`;
- authorization review run `31930484434` SUCCESS;
- repo contract `31930484355` SUCCESS;
- allocation marker written once and allocation-lock/dispatch created only after the exact-head authorization/freshness checks passed;
- after terminal result binding, PR #225 was **CLOSED, Draft, UNMERGED** and must remain immutable/do-not-reuse evidence.

### 34.6 Ordinal28 scientific execution — TERMINAL PASS

Scientific run:

`31930614067`

Attempt:

`1`

Exact authorization/dispatch head:

`54d7e35c45b6641065fc6670464e255bb790e102`

Outcome:

- preflight SUCCESS;
- 24/24 protected case jobs SUCCESS;
- exactly 24 syntax checks total;
- exactly 24 MYSTIC solver invocations total;
- 960,000,000 configured photon histories;
- all seeds `2110000001..2110000024` consumed and permanently non-reusable;
- no GitHub rerun/retry/resume;
- evaluation job SUCCESS;
- independent result self-hash / immutable-identity audit SUCCESS.

Evaluation artifact:

- artifact ID `9259277932`;
- digest `sha256:a1227bb40b28d9c89b73c62516e1da099d07dcb53829e99fdc54f49fd7877d8f`;
- downloaded ZIP SHA-256 independently matched the GitHub digest exactly;
- result file byte SHA-256 `3f34d4ab7e83e763393f5a8e02eef0bc7f2ef41688208b24656493a7afd02ee4`;
- result canonical/self SHA-256 `205b45d416ea6a8a55e5032f40ed6a3b31f0db55add6bd60d6581dbde6c809ba`.

**Terminal scientific verdict:**

`PASS_FROZEN_FRESH_DOD`

`definitionOfDonePassed = true`

All frozen gates passed:

- support: PASS;
- aggregate primary MALE: `0.1325690585585166`;
- same-truth frozen-training-mean baseline MALE: `1.9098114993441204`;
- surrogate/baseline ratio: `0.06941473470237472 <= 0.70` — PASS;
- shape median NRMSE: `0.5143930188255337 <= 0.75` — PASS;
- shape worst NRMSE: `0.7272587196183139 <= 1.25` — PASS;
- worst single normalized shape coefficient error: `1.9205612510909926 <= 3.0` — PASS;
- photopic: abs mean signed log bias `0.07811847363756959`, median abs log error `0.10283458533026224`, worst abs log error `0.27041491794075334`, worst uncertainty-normalized error `2.253372559614904` — all PASS;
- scotopic: `0.06747267742190469`, `0.11025852677675813`, `0.2374131950405629`, `1.9783931134121933` — all PASS;
- Johnson-V: `0.07751846311476507`, `0.10333351674930191`, `0.2683675886957957`, `2.236309766755406` — all PASS.

Isolation checks in the terminal result:

- `ordinal22ValuesRead=false`;
- `ordinal27ValuesRead=false`;
- `ordinal27MayInfluenceResult=false`;
- `retuningPerformed=false`;
- `modelChangedAfterHoldoutOpening=false`;
- `p90OrP95Used=false`.

Ordinal28 protected values are now opened/consumed validation evidence for **this exact frozen model only**. They may not be reused as fresh validation after any future model change/retuning.

### 34.7 Immutable ordinal28 PASS result binding

Issue #60 terminal PASS checkpoint:

`5306087120`

PR #226 — `Bind terminal Level-B v3 ordinal28 fresh-validation PASS`:

- exactly one result-binding JSON file;
- head `44f95cf92c0121b2f2e87169cdefb7c4227b40e8`;
- repo-wide contract run `31931170880` SUCCESS;
- merged as `aace88e6c311aae9cb6a7b4879cfb608972aae26`;
- path `review/level-b-v3-fresh-validation-ordinal28-result-v1/result-v1.json`;
- Git blob `96602789f5a8053e9717c4f899fe54e4c057608e`;
- binding self SHA-256 `120a4649ad61159c4d4edc13f10dd8ca335408dc2dcc3b9c0889bbced2485c57`.

After #226 merged, authorization PR #225 was closed Draft/unmerged with an explicit terminal do-not-merge/do-not-reuse closure.

### 34.8 Computationally validated surrogate package — canonical consumer artifact

0072 allows a separate **computational publication/package handoff** after PASS; it does not automatically open production.

PR #227 — `Publish computationally validated Level-B v3 surrogate package`:

- review-only publication/consumer packaging;
- no fitting/retuning;
- no ordinal28 protected truth/case-artifact access;
- no MYSTIC;
- no support/DoD/runtime/physics change;
- four added package/review files, zero deletions;
- review head `eb084a7ea292660e158f17d68a23850edea9403a`;
- dedicated package run `31931494213` SUCCESS;
- repo-wide contract run `31931494199` SUCCESS;
- review artifact `9259421804`, digest `sha256:d962f39bef6844024d3d4581e678b08d96dfa534705fecc883380bf1fa4615df`, independently reverified;
- merged to main as `0773ef6acff167b17acdcd30f52a5190b3c2280b`.

The same package workflow then ran automatically on canonical main:

- push-to-main package run `31931659430`, attempt 1, SUCCESS;
- canonical package artifact ID `9259468043`;
- canonical artifact digest `sha256:ed7c62c3efea525c531ab6587108320f5be5546d210af5054a5304ed07939a39`;
- artifact size 846,488 bytes;
- independent downloaded ZIP SHA-256 matched the GitHub digest exactly;
- package contains **exactly three files**:
  1. `validated-surrogate-package-v1.json`;
  2. `model-artifact-materialization-v1.json`;
  3. `spectral-representation-v2.npz`;
- package manifest canonical/self SHA-256 `2df88b800483127d565e66b03a5773920dd6a687f9afb0ace43f3cc93b2635aa`;
- package manifest file SHA-256 `5c3d4d098f6a6211994d9c84a41dced2e63ec242169a7230128cab90ddeaebf9`;
- model file SHA-256 `0b850664584244abdc781f87ce9e5b89cdab28b08a2999612b155378cbe42d79`;
- representation file SHA-256 `2491ac91ed924f2ba69b37ea20f48d63f51d41146cd9fe50e0bd63bfb315a763`.

The package deliberately includes **no** training dataset, candidate-selection table, ordinal28 case artifact, ordinal28 protected truth record, raw MYSTIC spectrum or executor.

Reference prediction contract:

- hybrid engine: `review/level-b-v3-training-only-implementation-v1/engine_v1.py`, Git blob `8b09967f370699083a857dc1f7078c1fc0a443bf`;
- frozen base predictor: `review/level-b-v2-training-implementation-v2/train_v2.py`, Git blob `bd0d20ebaaf77a8780dbfa021cfaa49bf3e2d0be`;
- exact definition: `engine_v1.predict_hybrid(materialized_model.model, geometry, train_v2.predict)`;
- float64, no randomness.

Consumer geometry inputs are exactly:

`sunDepressionDeg, targetAltitudeDeg, relativeAzimuthDeg, observerElevationM, aod550`.

Validated physical design box remains:

- Sun depression 2.0–10.5°;
- target altitude 5–80°;
- relative azimuth 0–180°;
- observer elevation 0–2500 m;
- AOD550 0.05–0.40.

Validated computational support requires **both** the design box and nearest frozen-training `V1_IDW_COS` coordinate distance `<=0.60`. Silent extrapolation remains forbidden.

Prediction output is 13-dimensional:

- indices 0–2 = natural log of photopic luminance, scotopic luminance and Johnson-V effective radiance; recover positive physical channel values with `exp`;
- indices 3–12 = frozen nullspace PCA coefficient means normalized by their frozen component scales;
- primary residual correction affects only indices 0–2; shape predictor is unchanged from the frozen v2 base.

Package consumer boundaries are explicit:

- computational surrogate-vs-direct-MYSTIC validation **PASSED** for the exact model/hash;
- measured real-sky validation is **NOT complete**;
- human first-seeing validation is **NOT complete**;
- empirical uncertainty/coverage calibration is **NOT complete**;
- `workerBIntegrationAuthorizedByThisPackage=false`;
- `workerCReactivationAuthorizedByThisPackage=false`;
- `productionPromotionAuthorized=false`;
- any future runtime port must preserve the exact model/package/support identities and prove numerical prediction parity before any shadow or production use;
- ordinal28 values may not be used for runtime retuning.

Issue #60 computational package checkpoint:

`5306134687`

### 34.9 Application repository live state after computational package completion

Repository: `search-maker/starsvisibility`

At the post-package refresh:

#### PR #24 — parent scientific/application reference branch

- OPEN;
- Draft;
- branch `science/visibility-v3-blackwell-crumey`;
- head `e676056d8c896a72bf37dd803becdf07a8cc71da`;
- 204 commits / 122 changed files;
- not production-wired.

#### PR #48 — Worker-B runtime integration foundation

- OPEN;
- Draft;
- mergeable;
- branch `science/level-b-runtime-integration-v1`;
- parked head `64a27433766e778b0bb8ef5573b81e18989698c6`;
- 18 changed files / 2,042 additions / 0 deletions;
- provider-neutral atmosphere/runtime foundation and automatic AERONET/CAMS acquisition remain present;
- previous GitHub Actions attempts on this parked head had no assigned runner and executed zero deterministic test steps;
- **the new validated package does not automatically authorize editing/reactivating this lane under 0072.**

#### PR #49 — Worker-C empirical validation infrastructure

Remains parked. No synthetic infrastructure result is real empirical validation, and no human/real-sky validation campaign has been authorized merely by the ordinal28 computational PASS.

### 34.10 Current finite Level-B gate interpretation after ordinal28 PASS

The critical computational surrogate gate has materially advanced:

- surrogate vs direct MYSTIC on a genuinely fresh, preregistered protected source: **PASS for the exact v3 model**;
- package for exact model/representation/support consumption: **published and independently hash-verified**.

Still **not complete**:

1. measured directional real-sky validation with useful atmosphere metadata;
2. final same-atmosphere application integration/parity against the packaged surrogate;
3. protocol-specific human calibration with detections, non-detections and catch trials;
4. untouched end-to-end first-seeing session validation;
5. empirical uncertainty/coverage calibration;
6. reviewed shadow/production promotion.

Therefore never say “Level B is fully validated” or “production-ready” merely because ordinal28 passed. The correct statement is:

> **The exact frozen v3 twilight-sky surrogate has passed its preregistered fresh direct-MYSTIC computational validation and has a canonical consumer package. Real-sky, human/end-to-end, empirical-coverage and production gates remain separate.**

### 34.11 Immediate next admissible transition

`MYSTIC-STATE-0072` computational execution/package work is now complete.

The next highest-value engineering transition, if the project continues, is a **new governance directive** for the narrow Worker-B application integration lane. Such a directive should, before any app write:

- bind canonical package artifact `9259468043`, digest `sha256:ed7c62c3efea525c531ab6587108320f5be5546d210af5054a5304ed07939a39`, package manifest SHA `2df88b800483127d565e66b03a5773920dd6a687f9afb0ace43f3cc93b2635aa`, model SHA `c4902eb3c2ba67b12dc4ef2b9cefb67c5963a6abc104708a73b2aab5dd0163b9` and representation SHA `2491ac91ed924f2ba69b37ea20f48d63f51d41146cd9fe50e0bd63bfb315a763`;
- authorize only a thin runtime adapter/port on the parked Worker-B lane;
- preserve the validated support predicate and fail closed/OOD behavior;
- require parity against the Python reference predictor on non-protected known-answer/synthetic/training geometries before any application shadow use;
- forbid model retuning, ordinal28 reuse for fitting, threshold relaxation and production-default changes;
- leave Worker-C/empirical campaigns separately gated.

Do not silently reactivate Worker-B or production under 0072; create and record the next governance state first.

### 34.12 Identity/non-reuse ledger additions

Never rerun/retry/resume or reuse:

- ordinal28 run `31930614067`;
- ordinal28 authorization head `54d7e35c45b6641065fc6670464e255bb790e102`;
- ordinal28 authorization/allocation/dispatch refs;
- seeds `2110000001..2110000024`;
- ordinal28 protected truths as a “fresh” source after any future model change.

Preserve as immutable:

- 0072 directive comment `5305940266`;
- terminal PASS checkpoint `5306087120`;
- result-binding merge `aace88e6c311aae9cb6a7b4879cfb608972aae26`;
- package merge `0773ef6acff167b17acdcd30f52a5190b3c2280b`;
- package checkpoint `5306134687`;
- canonical package artifact `9259468043` and its exact digest/hash identities above.

This appended section is the authoritative live override for all earlier dynamic-status text in this cumulative handoff unless a later Issue #60 governance directive and later handoff append explicitly supersede it.
HANDOFF_DELTA_EOF
          git diff --check -- "$f"
          git config user.name 'search-maker'
          git config user.email '304153003+search-maker@users.noreply.github.com'
          git add "$f"
          git commit -m 'Update cumulative handoff through ordinal28 PASS'
          git push origin HEAD:docs/update-2026-08-16-handoff-post-0072
