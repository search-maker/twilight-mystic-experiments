name: Twilight surrogate tier-1 atm z grid equivalence proof staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging-only:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse automatic proof execution during atomic staging
        run: |
          echo "PR-triggered proof is intentionally disabled while code, tests, and final workflow are staged."
          echo "No uvspec or MYSTIC execution occurs in this staging commit."
