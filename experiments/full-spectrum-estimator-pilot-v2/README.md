# Full-spectrum estimator pilot v2 disabled transport v6

Status: **review-only disabled transport; not authorization**.

This reconstruction is a new identity because the historical v5 ZIP bytes are unavailable. It is byte-bound to the merged PR #109 review package through `review-binding.v6.json`; it does not claim or reuse any v5 hash.

Hard boundary: no MYSTIC/libRadtran/uvspec invocation from transport review or authorization review; no ordinal-14 allocation/reservation/consumption; no dispatch; no fitting/model selection; no holdout/validation; no Tier-2/production transition.

A future enabled authorization must be a one-file Draft/open/unmerged same-repository child of then-live `main`. The document binds its parent and keeps `exactAuthorizationCommit` null. The authorization HEAD is bound externally by Git metadata, an exact attempt-1 zero-runtime authorization-review run, the exact Issue #60 marker, and only later the dispatch ref.

Frozen reconstruction identities are generated in the package manifest.
