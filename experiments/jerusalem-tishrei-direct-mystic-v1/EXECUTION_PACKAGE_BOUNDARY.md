# Jerusalem Tishrei direct-MYSTIC execution-package boundary

This package remains non-authorized by default. `authorization.cross-geometry.json` is disabled; no syntax check, `uvspec`, MYSTIC solver, or workflow dispatch is authorized by the presence of these files.

Before any future authorized case can reach syntax or solver execution, the frozen execution adapter now verifies the rendered aerosol directive fail-closed: there must be exactly one `aerosol_set_tau_at_wvl` directive and it must bind the normalized AOD to wavelength 550 nm. For the preregistered Tishrei event the normalized AOD550 remains 0.22.

The package validation workflow must pass the exact 3-geometry / 12-case / 240M-photon guard and plan in a locally simulated one-purpose authorization commit while executing zero solver and zero syntax-check processes. F remains 3.14; no parameter tuning, production claim, measured-real-sky claim, full-spectrum Level-B claim, human-first-seeing claim, or Pandora authorization is introduced here.
