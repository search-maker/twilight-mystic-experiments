# Tier-1 precision continuation wave 2 v1

This package prepares the second precision-continuation wave for the exact 16 geometries classified `ADAPTIVE_CONTINUATION_REQUIRED` by the independently audited, merged-main ordinal-11 salvage.

## Frozen scope

- blocks: b5-b6 only;
- geometries: 16 exact IDs from the main-bound salvage analysis;
- cases: 32;
- maximum configured photon histories: 4,600,000,000;
- roles: 14 surrogate-training geometries and 2 internal-holdout geometries;
- physical inputs, per-geometry photon budgets, roles, thresholds, stopping rule, and zero-hit treatment are unchanged;
- all b1-b4 evidence is immutable.

The b5-b6 seed table in `evidence/tier1-precision-continuation-wave2-v1/seed-plan.json` is the exact subset preregistered in the original reviewed v2 proposal before any continuation result was visible. Those 32 seeds have never been dispatched or consumed. Reusing this frozen, unused subset preserves the proposal hash required to combine the audited b3-b4 wave with b5-b6. The package proves zero overlap with the 196 pre-ordinal-8 historical seeds and the consumed ordinal-8 through ordinal-11 wave seeds; the remaining 48 preregistered b5-b8 seeds remain untouched.

## Candidate identity

Ordinal 12, key `twilight-surrogate-tier-1-v1:numerical:12`, and title `Tier-1 precision continuation wave 2 ordinal 12` are review-only candidates. They are not allocated, reserved, authorized, or dispatched by this package. A fresh duplicate search and a separate one-purpose authorization commit are required before any scientific execution.

## Source binding

`source-salvage.json` binds main `4ebc7c0eef2e12562e5a1a9bed857c0fbf2c10f1`, postprocessing-only run `31063167217`, artifact `8952843354`, and exact aggregate, audit, analysis, report, and source-inventory hashes. Contract CI independently downloads that artifact and verifies the payloads rather than trusting the descriptor alone.

## Closed boundary

Preparation and contract CI perform no solver execution, workflow dispatch, retry, rerun, resume, fitting, internal-holdout opening, Tier-2 action, or production promotion. PR #81 remains unmerged historical authorization evidence, and ordinal 11 plus its 40 seeds can never be reused.
