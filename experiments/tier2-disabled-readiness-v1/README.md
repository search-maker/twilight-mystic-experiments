# Tier-2 disabled readiness v1

Proposal-only infrastructure for the predeclared Tier-2 completion design.

Frozen boundary:

- exactly 48 additional geometries and 96 cases;
- exactly two blocks per geometry;
- exactly 7,320,000,000 configured photon histories;
- deterministic regeneration from the frozen training design;
- roles, parameter space, holdout partition, photon schedule, and anchor separation remain unchanged;
- no active authorization, dispatch, model fitting, automatic trigger, or production promotion.
