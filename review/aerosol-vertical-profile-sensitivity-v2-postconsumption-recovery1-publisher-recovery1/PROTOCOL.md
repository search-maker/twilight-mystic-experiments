# AVPS v2 ordinal-42 publisher recovery1

Status: **pre-consumption transport recovery only / zero runtime / result closed**.

The already-reviewed original ordinal-42 publisher run `33257846884` attempt 1 failed in its zero-runtime pre-dispatch fence before creating the dispatch ref, before posting the consumed marker, before uploading publisher evidence, and before requesting the science workflow. Its exact failure is a mechanical relocation error: the authorization-bound seed ledger was copied out of its native repository path, so its `__file__`-relative `ROOT` resolved above the checkout and `build_skeleton.py` could not be found.

This package does not change any AVPS v2 scientific input, seed derivation, ordinal, case, CRN group, profile state, photon budget, analysis rule, result-opening rule, or science workflow byte. Ordinal 42 remains the reviewed fresh identity under PR #629; ordinal 41 remains permanently consumed. The only repair is to materialize the exact authorization head as a detached local worktree and execute the exact seed-ledger blob `491d1b6653bea0fcc5275269723a76aa1af52300` from its native path before repeating all tracked-tree/repository-global collision checks and live authorization/no-dispatch/no-consumption/no-science-run fences.

The recovery publisher must fail closed unless the failed publisher run/job provenance is unchanged; the original artifact-derived science workflow remains exact SHA-256 `04a178e0cc777e0fc090c8bfc6175ad10b4bec56c6f12048428382ea36217878` / Git blob `7af7f28269ad245b1b9218012707957b048a6875`; authorization PR #629 and its attempt-1 review remain exact; Issue #60 still contains exactly one allocation marker and no ordinal-42 consumed marker; the exact dispatch branch is absent; no recovery science workflow_dispatch exists; and fresh seed collision scans remain zero.

Merging this package does not itself activate the recovery publisher. After exact-head read-only review and merge, activation requires a separate one-file branch/marker commit on `dispatch-trigger/aerosol-vertical-profile-sensitivity-v2-postconsumption-recovery1-ordinal-42-publisher-recovery1`, parented directly by then-current main, which may request the fresh recovery publisher exactly once. The failed publisher workflow/run must never be rerun, retried, or resumed. Taylor/Jerusalem residuals, protected holdouts, Level-B admission and production remain outside this gate.
