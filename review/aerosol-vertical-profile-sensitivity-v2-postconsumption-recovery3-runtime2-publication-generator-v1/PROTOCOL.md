# AVPS v2 recovery3 ordinal-44 runtime2 publication generator gate

Status: **ZERO-RUNTIME REVIEW ONLY**. Controlling defect: Issue #60 comment `5470658421`.

The recovery3 ordinal-44 authorization remains Draft/open and unconsumed at PR #718 head `dd3a4c692af505389e9feb1e5f5480fa389110a3`. The old package inherited from main `e9f79772f07e2a90974979f187be137606c3dfea` remains `NOT_ADMISSIBLE`/`DO NOT USE` for dispatch because its science workflow calls executor and aggregator identities frozen to ordinal 41. Publisher run `33329476520` attempt 1 remains immutable pre-consumption failure evidence and must never be rerun.

The reviewed zero-runtime runtime-identity gate merged as main `8e7498f58b219467ee3a17f9503a5b68b7d105eb`. Its executable review proved exact recovery3 authorization/guard/head+PR/ordinal/key/seed/branch acceptance, old ordinal-41 refusal, exact 360-case / 72-CRN / five-state structure, and exact closed-aggregate 360-case acceptance with 359-case refusal. The reviewed wrapper byte identities are fixed in `generate.py`.

This generator gate prepares a **new publication identity** without executing science. The generated publication consists of exactly six files: three reviewed runtime wrappers under `runtime-avps-v2-recovery3-ordinal44-v1/`, plus new runtime2 science, publisher and trigger workflows. The science workflow differs from the old package only in transport identity/name wiring: it routes case execution and aggregation through the reviewed recovery3 wrappers. The publisher preserves the already-reviewed snapshot-fence choreography while rebinding the controlling defect to `5470658421`, requiring a new post-publication admissibility marker prefix, and requiring the new exact publication-review receipt. The trigger uses a fresh activation branch/marker and binds the exact new publisher blob.

Hard boundaries: no change to the frozen 360 cases, 72 CRN groups, five profiles, 20,000,000 photons/case, case inputs, geometry, spectra, estimator, thresholds, recovery3 seeds or authorization. No dispatch branch/consumed marker is created. No libRadtran/MYSTIC/uvspec executes. No result, Level-B result, protected holdout, Taylor/Jerusalem residual, or invalidated low-alt evidence is opened/used. Every richer `newMappingAuthorized` remains false.

The generator review must prove deterministic generation twice, exact reviewed wrapper hashes, exact six-file publication set, recovery3-only executor/aggregator routing, old ordinal-41 runtime paths absent, fresh publisher fail-closed marker semantics, preservation of BEGIN -> bootstrap -> science dispatch -> read-only guard wait -> matching END snapshot choreography, fresh trigger identity, and executable acceptance/refusal fixtures from the merged runtime-identity gate. It must not execute any generated workflow.

After this gate is reviewed and merged, the generated six files may be published in a separate non-draft PR from live main. That publication must pass the separately installed exact-byte publication review before merge. Only after publication merge and a fresh Issue #60 read may one corrected-admissible marker with prefix `AVPS_V2_RECOVERY3_ORDINAL44_RUNTIME2_EXECUTION_PACKAGE_CORRECTED_ADMISSIBLE` be recorded. No dispatch is authorized by this generator gate or by publication review alone.
