# AVPS v2 recovery2 authorization-review infrastructure

This package adds a solver-free, read-only `pull_request_target` review for the one-file recovery2 successor authorization child proposed by the successful post-transport authorization-control recovery.

It is infrastructure only. It does not allocate an ordinal, create an authorization or dispatch ref, write an Issue #60 allocation/consumption marker, execute libRadtran/MYSTIC/uvspec, open AVPS results, admit richer Level-B atmosphere coordinates, access a protected holdout, use Taylor/Jerusalem residuals, or change production behavior.

The review binds exact control head `0842dd27f62c4bc2af4b5763ae4dd547ee009fce`, PR #646/run `33276052256`/artifact `9721690607`, requires the authorization PR to be an exact one-file direct child and byte-equal to the reviewed `authorization.json`, rebuilds the recovery2 72-seed ledger while validating consumed ordinal-42 bytes only at their native historical authorization-head path, repeats tracked-tree and repository-global seed scans, and dynamically checks the global ordinal surface while allowing only the candidate authorization branch/PR/runs themselves as successor-identity self-observations.

Ordinals 41 and 42 and all their seed/run/dispatch identities remain permanently consumed and non-reusable. The frozen science remains 360 cases / 72 CRN groups / five OPAC profile states / 20,000,000 photons per case. A passing review is still not allocation or dispatch. An exact Issue #60 allocation marker may be written only after the protected review run is terminal attempt-1 SUCCESS, its artifact is independently read back, and the matching WRITE_QUIET fence is explicitly closed.
