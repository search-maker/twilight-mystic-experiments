# AVPS v2 recovery3 ordinal-44 execution-package generator gate

This is a **zero-runtime mechanical transport/review gate**. It exists because ordinal 44 is now reviewed/allocated but the currently published AVPS science/publisher workflows are hard-bound to consumed recovery2 ordinal 43 and must never be reused for the fresh scientific identity.

The generator is bound to the exact reviewed recovery3 authorization identity: PR #718 head `dd3a4c692af505389e9feb1e5f5480fa389110a3`, parent `d8cd4af807e7a8f11ed39fdc579ed92adf866aab`, authorization-review run `33319037610` attempt 1, artifact `9734515864` (`sha256:bfef625ebb0a45f8a59e38cb46b64ded9e7d9f3fcbb895355144a3af5044eed7`), and Issue #60 allocation marker `5469582091`.

It mechanically derives a **fresh recovery3/ordinal-44** science workflow, zero-runtime dispatch publisher, and push-trigger bridge from the already reviewed recovery2 source workflows. The source workflow Git blobs are hard-bound. Only identity/provenance/seed-ledger/global-ordinal/package-review bindings are changed; the 360-case / 72-CRN-group / five-profile / 20,000,000-photon-per-case scientific design and the existing executor/aggregator/profile/runtime bindings are preserved.

The generated publisher includes the already-proven `GH_TOKEN`/`GITHUB_TOKEN` propagation correction and must bind the exact fresh execution-package review artifact before it can create a dispatch ref or consumed marker. The generator itself creates **no branch/ref, no dispatch, no consumed marker, no solver execution, no result opening, no Level-B admission, no holdout access, no Taylor/Jerusalem use, no invalidated low-altitude evidence use, and no production change**.

After this generator/review gate is merged, a separate PR may publish exactly the three generated workflow files. The same review workflow will regenerate the package from main and require byte-for-byte equality before any later one-use publisher activation is considered. PR #718 remains Draft/open/unmerged.
