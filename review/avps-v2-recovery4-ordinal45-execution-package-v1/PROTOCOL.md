# AVPS v2 Recovery4 ordinal45 execution-package review

Status: **REVIEW ONLY / ZERO RUNTIME / NO DISPATCH**.

This review is bound to the already-reviewed and separately allocated Recovery4 ordinal45 authorization commit `6e095b4b1603c90dcee0943295909b30cd1b374d` (PR #774), its successful authorization review run `33368601918` / artifact `9749469156` / digest `sha256:1d7d8d62f5372b989a9bbb2b9546598de4dac350adeb4c00db4712420be5e751`, and exact candidate identities `ddded6b2d170ca2fac8d498bdba2887446c16995df0880d948fb2be00870b3de` / `c439de417520b330c037e2628df02b6955f652563300aa5ef30477abf7661a98`.

The former exact-head package artifact `9750420831` and generated science-workflow blob `b620f9e554378ec363ec1cc3da860432b7615218` are **review evidence only and not publication/dispatch admissible**. Independent audit comment `5475701854` found a temporal-fence defect: the repository-global candidate-seed scan could run before the science WRITE_QUIET BEGIN was proven, while the later release barrier independently searched for a BEGIN+END pair. A BEGIN created only after the scan could therefore have satisfied the old release path.

This fresh-child correction changes only mechanical temporal-fence enforcement and its zero-runtime review fixtures. The generated preflight must fetch the complete Issue #60 ledger **before** `repository_global_seed_scan.py`, require exactly one globally-unmatched Recovery4 ordinal45 BEGIN bound to the current authorization, dispatch branch, workflow, execution key and science run, persist its exact comment ID/token/timestamp, and record that binding in the uploaded execution guard together with scan start/completion timestamps and the scan digest. The release job must download that exact current-run preflight guard and accept only the matching END for the already-bound begin ID/token/current science run after the successful scan; it must not independently discover a later BEGIN.

The zero-runtime fixture suite must prove: a pre-scan matching BEGIN is accepted; absence of a BEGIN at scan time is refused even if a later BEGIN could exist; an exact END for the bound begin/current run is accepted; a wrong begin ID is refused; and a wrong current-run END is refused. The generated manifest must be deterministically rewritten to the corrected workflow hashes and explicitly record the pre-scan binding/release semantics.

All science is otherwise byte/semantic-preserved from the reviewed package architecture: 360 cases, 72 common-random-number groups, five vertical-profile states, 20,000,000 photon histories per case, the same wavelengths/geometries/profiles/runtime/source identities/estimator/aggregation, and four solver-bearing shards each retaining `needs: [preflight, snapshot-fence-release]`. Ordinals 41–44 and all consumed identities remain non-reusable. No Taylor/Jerusalem residuals are read or usable.

This review creates no dispatch branch, consumed marker, solver run, seed application to tracked cases, result opening, richer Level-B mapping, protected holdout opening or production transition. Every richer OAS-v2 `newMappingAuthorized` remains false. Publication of corrected exact bytes, dispatch activation, WRITE_QUIET science snapshot and one-shot execution remain separate later gates.
