# OPAC + custom vertical tau composition capability v1

Status: **REVIEW ONLY — SYNTAX/CAPABILITY ONLY — NO SCIENTIFIC SOLVER — NO ORDINAL**

## Question

Can the exact project libRadtran runtime accept the intended future vertical-profile isolation surface in one input:

```text
aerosol_default
aerosol_species_library OPAC
aerosol_species_file continental_average
aerosol_file tau <custom normalized vertical optical-depth shape>
aerosol_set_tau_at_wvl 550 0.10
```

The intended future scientific use would hold one physically coherent OPAC spectral/phase aerosol family fixed while varying only the normalized vertical optical-depth profile at fixed total AOD. This capability review does **not** authorize that science.

## Why this gate is required

The project already closed broad aerosol optical-property work:

- AOPS v1 / ordinal 37: controlled SSA and scalar-g sensitivity;
- AFPF v1 / ordinal 38: realistic full OPAC phase-function/family sensitivity;
- ASIV v1 / ordinal 39: fresh-holdout scalar and derived Level-B scenario transport;
- starsvisibility PR #100: verified shadow-only five-state aerosol scenario envelope.

The remaining atmosphere-physics gap is therefore not another generic SSA/g/family campaign. Taylor showed that normalized aerosol vertical structure can materially alter twilight radiance. A clean next experiment should vary vertical structure without simultaneously changing spectral SSA/phase family.

The libRadtran User Guide documents OPAC `aerosol_species_file`, custom `aerosol_file tau`, and `aerosol_set_tau_at_wvl` separately. The exact locked project runtime must prove that the combined directive surface parses before any scientific profile universe, seeds, photon budget or ordinal is frozen.

## Exact runtime binding

Reuse the previously validated AFPF OPAC runtime identity:

- `rubin-libradtran=2.0.6=py312pl5321he9373c2_1`;
- official `optprop_v2.1.tar.gz` archive SHA-256 `11daa1f1f4be0fd4ddf7e881ec2005498049674a1540d37b4b1e8f5e16052c7e`;
- archive size `743391266` bytes;
- base data-tree SHA-256 `ad30b49177e9c84e46497d69faf0c75e466996b0d0003f1de210289ae9f847d7`;
- staged augmented data-tree SHA-256 `5d8bbf8e6b91ec3d405dee36f21a94afbb6e5ec6cd67da2dd5dd541738199d80`;
- locked `uvspec` SHA-256 `2b9c7a69e4dfe4e77ade97148b2499b0a2c205c8d8000d3516a29344cc9d2fc3`.

The custom tau file must be generated by the merged generic profile-transport foundation, not by ad-hoc text generation.

## Frozen parser surface

Exactly one mixture is tested: `continental_average`.

This is **not** a scientific selection of continental aerosol as the truth. It is simply one already validated AFPF OPAC mixture used to test whether rich OPAC optics can coexist syntactically with a caller-supplied vertical tau profile.

The custom profile used for syntax checking is deliberately synthetic and has no scientific interpretation. It exists only to exercise the parser surface. It is remapped onto the exact AFGL-US atmosphere levels through the generic vertical-profile transport module and normalized to unit layer optical-depth sum.

The command is exactly:

`uvspec -c`

No non-`-c` `uvspec` execution is allowed.

## PASS rule

PASS requires:

1. exact base runtime and staged OPAC data-tree hashes reproduce;
2. the merged vertical-profile transport module is used to render the tau file;
3. the tau layer fractions sum to one and use the reviewed lower-bound convention with zero at the top;
4. the exact combined aerosol directive order above occurs once each;
5. `uvspec -c` returns exit code 0 without timeout;
6. no unexpected files are produced by the syntax check;
7. report records `scientificSolverExecuted=false` and `mysticExecuted=false`;
8. no seed, scientific ordinal, result endpoint, Taylor/Jerusalem residual, or production mutation is created.

## Interpretation boundary

PASS would prove only **exact-runtime parser/data-resolution compatibility** of fixed OPAC optics plus a custom vertical tau profile and fixed AOD normalization.

PASS would not prove:

- that any particular vertical profile is realistic;
- that `continental_average` is climatologically correct for any site;
- the magnitude of vertical-profile sensitivity;
- MYSTIC numerical behavior under the combined surface;
- Level-B mapping accuracy;
- empirical real-sky accuracy;
- production readiness.

Only after PASS may a separate review freeze an independently sourced vertical-profile sensitivity experiment.