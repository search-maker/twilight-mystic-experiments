# ARM ENA/SWS E0 successor query-only availability discovery v1

This is result-blind control-plane/acquisition metadata work only. It exists because immutable E0 run `34243462864` consumed its one-shot authority and returned `SOURCE_FILE_MISSING` for `2017-06-16_dusk` under the exact `enaswsC1.b1` one-day query contract. That result is not a scientific failure and does not establish whole-instrument absence.

The ordered input is frozen to the 25-case intersection already admitted by the pre-existing CSPHOT-side and E7 OMI gates. The exact source is E7 run `33467554248`, artifact `9785389875`, artifact SHA-256 `2c5aac079a0a945cedd6783f9c1eac56a37a9a62e6ac99505c4e0607abb765f4`, result CSV SHA-256 `875b89464ba8ce41faf27525adf9d5d1094783636323a4f1ee144a85366f3305`, governed in Issue #60 comment `5488625205`. The local frozen manifest `ordered_25_cases.json` has SHA-256 `8a0756be59dac59cd8ad4fab77e499ec19069e24d2d2a636fa4352713b550def` and preserves those 25 PASS rows in source chronology.

The authenticated executable may call only ARM Live `/query` for datastream `enaswsC1.b1`, one exact civil day per frozen case in that order. It may inspect only returned metadata strings needed to recognize exact native basenames matching `enaswsC1.b1.YYYYMMDD.*.(nc|cdf)`. It stops deterministically at the first case/day with one or more exact matching basenames, or exhausts all 25. An earlier query error fails closed and stops; it is never skipped in favor of a later case.

It has no `/saveData` path, no netCDF dependency, no native-file download/open function, and no protected value reader. Credential values enter only through inherited `ARM_USER_ID` and `ARM_ACCESS_TOKEN`, are never accepted on the command line, and are never persisted or printed. Output is one closed-schema sanitized JSON receipt containing case IDs/dates, match counts, and exact native basenames only; no URLs, raw query payloads, credentials, radiance/signal/count/intensity values, or native files may be retained.

`EXACT_EXECUTABLE_STRESS_PASS` is zero-ARM-runtime. It hashes the actual workflow/executable/manifest bytes, verifies dispatch/checkout/firewall semantics, reads the complete then-live Issue #60 ledger through GitHub only, and fails on an unmatched ARM WRITE_QUIET fence or explicit adverse ARM governance after Coordinator baseline `5588636524`. The authenticated query job depends on that stress job and is runnable only for `workflow_dispatch`.

This candidate and its CI do **not** authorize authenticated invocation. A fresh separate Coordinator protected-boundary transition is required before any `workflow_dispatch` that can contact ARM. Even after a query-only result, native-file download/open, protected SWS/SASZE value opening, Stage B, MYSTIC/science, production, and any identity/seed/ordinal action remain unauthorized.
