# ARM ENA/SWS E0 successor query-only availability discovery v1

This is result-blind control-plane/acquisition metadata work only. It exists because immutable E0 run `34243462864` consumed its one-shot authority and returned `SOURCE_FILE_MISSING` for `2017-06-16_dusk` under the exact `enaswsC1.b1` one-day query contract. That result is not a scientific failure and does not establish whole-instrument absence.

The first implementation candidate, Draft PR #1001 / head `0bc4793ae9c1f312e173d348a29e2f2ae3ddf9a2`, is governance-NONADMISSIBLE because it was created across a repository-wide WRITE_QUIET serialization window and must never be merged. Coordinator `5590748165` explicitly released the matching V5R1 fence and authorized only a wholly fresh ARM query-only successor preparation from clean `main`; it did not authorize authenticated invocation. The fresh successor was installed by PR #1003. Its postmerge stress run `34275610288` then failed closed on a mechanical false positive: positive Coordinator acceptance `5591488326` was treated as adverse because its body also preserved PR1001 nonadmissibility and protected-boundary prohibitions. Coordinator `5591521704` authorizes only the narrow result-blind stress repair described here; authenticated invocation remains false.

The ordered input remains frozen to the same 25-case intersection already admitted by the pre-existing CSPHOT-side and E7 OMI gates. The exact source is E7 run `33467554248`, artifact `9785389875`, artifact SHA-256 `2c5aac079a0a945cedd6783f9c1eac56a37a9a62e6ac99505c4e0607abb765f4`, result CSV SHA-256 `875b89464ba8ce41faf27525adf9d5d1094783636323a4f1ee144a85366f3305`, governed in Issue #60 comment `5488625205`. The local frozen manifest `ordered_25_cases.json` remains byte-identical with SHA-256 `8a0756be59dac59cd8ad4fab77e499ec19069e24d2d2a636fa4352713b550def` and preserves those 25 PASS rows in source chronology.

The authenticated executable may call only ARM Live `/query` for datastream `enaswsC1.b1`, one exact civil day per frozen case in that order. It may inspect only returned metadata strings needed to recognize exact native basenames matching `enaswsC1.b1.YYYYMMDD.*.(nc|cdf)`. It stops deterministically at the first case/day with one or more exact matching basenames, or exhausts all 25. An earlier query error fails closed and stops; it is never skipped in favor of a later case.

It has no `/saveData` path, no netCDF dependency, no native-file download/open function, and no protected value reader. Credential values enter only through inherited `ARM_USER_ID` and `ARM_ACCESS_TOKEN`, are never accepted on the command line, and are never persisted or printed. Output is one closed-schema sanitized JSON receipt containing case IDs/dates, match counts, and exact native basenames only; no URLs, raw query payloads, credentials, radiance/signal/count/intensity values, or native files may be retained.

`EXACT_EXECUTABLE_STRESS_PASS` is zero-ARM-runtime. It hashes the actual workflow/executable/manifest bytes, verifies dispatch/checkout/firewall semantics, and reads the complete then-live Issue #60 ledger through GitHub only. Its authority baseline is Coordinator `5590748165`, which must still contain the exact ARM fresh-successor preparation, PR1001 nonadmissibility, authenticated-invocation prohibition, and repository-wide WRITE_QUIET rule.

WRITE_QUIET remains repository-global across every owner lane. Every post-baseline `WRITE_QUIET_BEGIN` is tracked regardless of prefix; an END must name the exact `beginComment` and, when both sides state a `stage`, the stages must match. Any unmatched or misbound fence refuses the candidate.

Direct ARM governance is classified from the structured first-line transition identity before body history is considered. Explicit Coordinator acceptance/merge/install/repair authorization and `BLOCKER_CLEARED` titles are positive; ARM-owner review/checkpoint preparation titles are non-authorizing progress. This prevents a positive current transition from being poisoned by body text that merely preserves an older candidate's `NONADMISSIBLE` status or says authenticated invocation, ARM Live access, Stage B, science, or production remain unauthorized. Explicit title-level revocation, NONADMISSIBLE/NOT_ADMISSIBLE, merge prohibition, blocker/fail-closed, refusal, authority expiry, parking/read-only, quarantine, withdrawal, or cancellation remains adverse. Unknown direct ARM governance is ambiguous and therefore fails closed. Cross-lane Coordinator transitions are inspected only inside their explicit ARM section, where those same true adverse controls remain refusing.

The deterministic regression corpus includes the `5591488326` positive-acceptance shape, an ARM-owner ready-for-review checkpoint that references frozen PR1001 nonadmissibility, and nearby negative transitions using similar protected-boundary vocabulary but explicitly revoking, rejecting, refusing, or expiring the current candidate. Positive vocabulary never overrides an explicit adverse title.

The authenticated query job depends on that stress job and is runnable only for `workflow_dispatch`. This repair, its CI, installation preparation, and any later merge do **not** authorize authenticated invocation. A fresh separate Coordinator protected-boundary transition is required before any `workflow_dispatch` that can contact ARM. Even after a query-only result, native-file download/open, protected SWS/SASZE value opening, Stage B, MYSTIC/science, production, and any identity/seed/ordinal action remain unauthorized.
